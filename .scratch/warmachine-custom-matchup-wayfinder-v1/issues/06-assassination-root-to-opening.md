# 将真实刺杀终局根反推到合法开局

优化任务 `OPT-06` 已直接列于本工单的“开发补充”中；[原理审计](../../../docs/research/ticket-search-principles-audit-20260905.md) 仅保留依据与索引。`Blocked by` 表示最终完成依赖，历史验收范围不追溯改变。

Type: prototype
Status: closed
Blocked by: 02, 04
Part of: ../map.md

## 开发补充：OPT-06（2026-09-05）

以下为历史验收之外的维护/扩展待办，当前全部待实现/待验收；保留原完成状态，不要求重跑全部历史搜索。由相关开放工单集成时执行，不把它们新增成历史关闭门。

- [ ] 从历史刺杀路线抽取可复用前缀及其精确依赖；同前缀查询复用，历史/资源/窗口变化时重新验证。
- [ ] 增加读取历史证据的回归，检查被缓存的失败约束没有超出适用域；不为本优化全量重跑旧长搜索。
- [ ] 保留本ticket的存在性验收；完整动作域由20负责，全部对手策略由21负责，不能把后两项重新塞回06作为关闭条件。


## Question

怎样把一个已由 Ticket 05 和当前 Host strict 认证、且不存在提前终局的任务专属刺杀根接入多回合反向前沿，恢复一条动作、激活顺序、移动路径、底盘碰撞、资源、Chance、反应和历史损失都可执行的路线，最终与合法部署初始状态汇合并独立严格正向重放？

## Acceptance

- 反推从后续回合终局坐标开始，不把部署坐标替代终局假设。
- 每个前驱动作通过现有原子/钩子义务生成，并由 Host strict 正向执行验证；未知逆算保持 unresolved。
- 移动路线、激活先后、单位编队、模型碰撞、Fury/Focus、Frenzy、击杀生命周期和可选触发完整保留。
- 至少一条真实 Cryx/Fane 刺杀路线连接到合法部署，并在不读取旧回执作为执行输入的情况下独立重放到同一终局。
- 其它未展开位置、随机结果、对手选择和路线质量继续留在上界。
- 场景规则、终局物化和完整历史预设分别由 Host/Ticket 04、Ticket 05 和 Ticket 19 拥有；Ticket 06 只调用其 strict 门禁，拒绝路线中的提前终局，不在本工单补全场景规则或穷举比分历史。

## Progress

- [x] 补上历史中途胜利门禁：Steamroller 得分账本逐结算窗审计，整路 replay/reissue/prefix recovery 在声明终局边之前出现任何胜利都 fail closed。原始 `4:0` 状态在 player2 第 5 回合结束时已由 player1 场景胜利，不能原样恢复；动作见证只有在另一个明确的非终局历史假设下重新执行后才可继续使用。
- [x] 对七张 Steamroller 2026 场景逐图执行胜利阈值矩阵：对手回合结算后领先 2 分继续、领先 3 分立即结束、自己回合领先 3 分不结束。反推终局语料、前回合反推和整路 replay/reissue/prefix recovery 均对未声明的提前终局 fail closed。
- [x] v30 的 `2:0` 删除得分行方案已废弃。v31 曾保留 Cryx 全部旧得分并增加 player2 第 3 回合中央目标 2 分义务；它只通过五层局部重签，随后在第六层被 Host 结算出的 player1 `pressure-c` 1 分反证。固定后续得分窗要求早期分差同时满足 `d >= -2` 与 `d <= -3`，所以 v31/v33 只能作为失败诊断，不能作为可恢复路线。

- [x] 真实 `80` 模型刺杀终局已从后续回合坐标严格撤销终局攻击，并重建当前回合激活与控制阶段；不读取旧动作回执作为执行输入。
- [x] 多模型单位 advance/run、底盘碰撞、单位编队和路径阻挡由 Host 严格复验；Sepsira 从 `(12,20.25)` 到 `(12,12)` 的直线路径被场景旗帜阻挡，Host visibility graph 生成的三段绕行路径严格通过。
- [x] 修复激活/控制资源连续性。Sepsira 的控制阶段严格补到 `7 Focus`；反向激活通过 Host 枚举生成 `Excarnate(3) -> Breath Stealer(2) -> Breath Stealer(2) -> run`，三次进攻法术均绑定明确失败骰，花完资源后沿三段路径奔跑，得到与原目标逐字段相同的状态。
- [x] v6 layer 0 与 layer 1 检查点通过。layer 1 得到 `2` 个前一回合前沿，最深路径含 `39` 条严格反向边：终局攻击 `1`、控制阶段 `2`、上回合结束 `1`、pass `30`、普通/单位移动 `4`、资源前缀移动 `1`；控制阶段不再断链。
- [x] 修复最近部署槽被占用时错误消耗唯一候选预算，以及 Warjack/Warbeast 奔跑资源前像方向错误。明确预算调用可限制严格接受数和 Host 尝试数，但所有未执行组合必须保持 unresolved，默认调用仍完整枚举。
- [x] 真实 layer 2 使用 `2` 个隔离 worker 并行扩展两个 depth-1 前沿，父进程在全部成功后合并为 `4` 个不同 depth-2 前沿；检查点 `e729c432...`，最深路线 `58` 条严格边、其中 `6` 条整单位 run。任一 worker 失败时不得发布半检查点的 fixture 同时通过。
- [x] 将恢复粒度从“一个精确状态任务”细化到“一个完整激活标签”。检查点绑定激活根状态、阵营、语义选项、队列、已访问标签/状态、边界、展开账本和所有哈希；恢复最多重做当前标签，不拆分一次 Host 枚举或 strict transition。大检查点通过私有 gzip 内容哈希 spool 交给父进程，避免 Node IPC 复制阻塞，真实运行结束后残留文件为 `0`。
- [x] 修复真实第三层控制资源断链。Malefactor 的免费 run 使用控制范围圆、移动可达圆、模型排斥圆和桌边交点构成的有限角度事件单元，找到控制范围外的合法起点；未枚举半径深度继续记为 unresolved。空的 upkeep 支付审计数组不参与语义状态哈希，非空证据仍参与。
- [x] v14 layer 3 完成：旧 layer-2 路线先在当前 Host 下逐边重签 `58` 边/`76` 次新鲜严格转换，然后真实任务完成 `19` 个激活标签和 `1` 个激活边界检查点，得到 `2` 条历史标签、`1` 个精确 depth-3 状态。两条 `79` 边路线分别独立正向重放 `133/133` 次当前 Host 严格转换并抵达同一刺杀终局，不读取旧回执作为执行授权。
- [x] 反证旧终局账本：`4:0` 在 player2 第 5 回合结束时已满足场景胜利，不能继续到第 7 回合刺杀。该路线的动作边可作为局部诊断，但不再是完整合法对局证据。
- [x] v18 layer 4 完成：共享精确状态只扩展一次，`18` 个激活标签检查点全部提交，得到 `8` 条 depth-4 历史前沿。最深路线 `98` 条严格边，包含 `21` 条移动和 `9` 条整单位 run；保留 `294` 个 unresolved 与 `57` 个 strict reject。修正完整 `4:0` 账本与执行源后，旧 `98` 条边通过 `161` 次当前 Host 严格转换全部重新签发，而不是重新搜索终局到 layer 4。
- [x] v23 layer 5 完成：从已重新签发的 layer-4 检查点继续，一个精确状态任务提交 `20` 个激活检查点，约 `63` 分钟后得到 `2` 条 depth-5 历史前沿、`1` 个精确状态。最深路线 `119` 条严格边，包含 `29` 条移动和 `11` 条整单位 run；`461` 个 unresolved、`17` 个 strict reject，尚未抵达合法部署。Sepsira 使用目标/射程事件起点，在移动前对 Vordak 严格执行 `Excarnate(3) + Breath Stealer(2) + Breath Stealer(2)`，再 run 到观测位置；不能从射程外终点伪造资源消费。
- [x] v31 条件下的旧五层局部重签仍是有效诊断：一条等价行为路线的 `119/119` 边通过 `211` 次当前 Host 严格 transition，八个标签也满足同语义状态和同有序执行输入；但这些事实没有证明更早历史可达。第六层严格结算已使 v33 失去继续资格，原始 v23/e62497d4 同样不能单独恢复。
- [x] 冻结已被比分矛盾反证的第 7 轮 Pressure Point 长路线，并用 78 模型 Two Fronts 短路线完成存在性验收。组合预检先分别验证三段移动、三种控制资源前像、两个跨回合连接，再把 player2 首回合 `17/17` 个激活组整体反推到 strict 合法部署；完整搜索随后只处理 `3` 个主标签即得到 `1` 条合法部署路线，独立 current-Host strict replay 与 oracle 隔离均通过，报告哈希 `7ed036e32a003d8141d62166ac003691596240c5d335024b252d7602f3b90b00`。
- [ ] 将三次失败法术的 Chance 质量接入 Ticket 08 的精确概率账；当前动作与骰值完整保存，但不得把该单 witness 频次称为自然概率或胜率。
- [ ] 继续公开未展开激活顺序、单位移动锚点、资源消费动作族、目标和连续路径质量；它们保持 unresolved，不得由该 witness 排除。
