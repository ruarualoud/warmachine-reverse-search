# 建立规则与卡牌更新的增量重建流程

优化任务 `OPT-12` 已直接列于本工单的“开发补充”中；[原理审计](../../../docs/research/ticket-search-principles-audit-20260905.md) 仅保留依据与索引。`Blocked by` 表示最终完成依赖，历史验收范围不追溯改变。

Type: task
Status: open
Blocked by: 03, 04, 05, 20, 21
Part of: ../map.md

## 开发补充：OPT-12（2026-09-05）

以下为本工单剩余开发及新增验收，当前全部待实现/待验收，不因写入计划计完成。

- [ ] 同时记录已执行依赖和候选发现依赖：某次查询未发现反应能力也要绑定查询范围，不能只记曾经执行过的原子。
- [ ] 加入新增可用对手回应、删除/修改规则、只更新素材三个增量实验；前两类使有关动作域/剪枝/值失效，纯素材更新保留语义证据但更新展示。
- [ ] 在小域比较增量重建与干净全重建的集合、概率和声明；配合15复用内容指纹，实现只影响必要依赖的恢复。


### 策略工作台接入补充（待开发）

范围及公式见[策略工作台合同](../../../docs/STRATEGY_WORKBENCH_SCOPE.md)，本节是实际开发清单，不改变历史完成范围。

- [ ] 建议缓存同时绑定当前状态、已揭示信息、任务目标和剩余时域；实战新动作使旧建议标记过期。仅在完整依赖相符时复用子图，避免返回另一回合或另一目标的旧最佳动作。

## Question

当新卡、配装、规则文本、原子、钩子、场景或 Host 代码更新时，怎样只重建受影响的军表、需求组、终局根、反向路径和值报告，并证明未受影响证据可以安全复用？

## Acceptance

- 复合收据分别绑定数据、Force Builder、Host、原子/钩子、interaction graph、场景和搜索配置。
- 语义影响索引能区分纯构筑变化、既有原子复用、新语义、规则执行变化和场景变化。
- 只有依赖闭包未变且内容哈希一致的军表池、终局根、路径和报告可复用；其它证据 fail-closed 失效。
- 新增卡若复用既有原子仍需真实卡牌 strict 正反场景；新增语义在实现前保持规则未知。
- 用一次模拟构筑变化和一次规则原子变化证明增量重建范围、旧检查点拒绝和最终报告更新。
