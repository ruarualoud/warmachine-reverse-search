# Tasks

## 2026-09-05 全工单原理审计与开发顺序

- [x] 将OPT-01至OPT-23直接展开到各ticket的“开发补充”，分别列开发步骤与验收，报告降为索引；保留14完成/9开放。20显式依赖23最终规则认证，21分21a三个纵向片与21b完整域收口，22按需精化；已完成08及其它历史工单的补充是范围外维护，未被勾为实现。
- [x] 在Ticket21内明确对手回应：按Host真实owner/时点展开即时与完整回合的适应性选择，禁止未来骰值泄漏；预设终局根与任务目标分离，刺杀/得分并集不能相加各根min值，真实终局不生成死后回应。记录R21-01至R21-12待验收矩阵；本轮未改Search算法、Engine、来源账本或运行收据。
- [x] 本轮文档检查通过：23张ticket状态与修改前一致、76项OPT清单保持未勾选、12行R21验收齐全、24个本地链接有效、最终依赖图无环，`git diff --check`通过。既有7格初始值回归与17状态策略商回归通过；它们只复验已有基础，不宣称新增R21矩阵或多回合展开已完成。

- [x] 逐一核对23个当前Wayfinder工单、历史完成范围及现有值/几何/依赖实现；把过期18工单公开计划更新为23工单，纠正地图Ticket23为8/12。保留14完成/9开放，不因审计修改增加完成数。
- [x] 补充PET2、可靠区间迭代、符号几何规划、冲突回流、NBS、XDO、动态依赖与PRISM目标语义研究，写入 `docs/research/ticket-search-principles-audit-20260905.md`；每张ticket链接其OPT子任务。
- [x] 复跑17状态策略商回归与66候选三法构造实验；6/6有效类、隐藏优解补回及对手/随机分布隔离通过。58/9根计数不解释为真实整局速度。
- [x] Engine独立提交 `d29fa88` 完成Defensive Line估时试做及v1.1规范：2来源/1语义/1原子/0新原语，实测归类为接线+组合。当前三族账本剩余213（不重复归属53/152/8），未在Search复制规则、刷新认证收据或晋级旧结果。约46分钟端到端样本含穿插文档与返工；下一批3种简单语义暂按2-4小时预算，不能外推全任务工期。
- [ ] 实现任务级规则/查询域收据与20a/21a/22的可靠局部区间链。当前只是开发计划已分阶段，生产隔离门未放行。
- [ ] 按审计的OPT-01至OPT-23执行后续优化；完成范围以实际证据更新，不以文档勾选或微型通过代替。

## 2026-08-26 Ticket 23 rule semantics recertification

- [x] Create the independent Wayfinder Ticket 23 plan for rule semantics authority, primitive recertification and a computed Strict gate. The ticket quarantines current global Strict/skill/search claims without deleting historical evidence, separates official-source semantics from implementation and oracle, and defines twelve end-to-end slices from baseline quarantine through auto-hit/auto-miss, timing, Unit/combat, geometry, resources, scenario, faction rules, interaction/mutation closure and one canonical runtime receipt.
- [x] Complete Ticket 23 Slice 23.0. Engine now emits deterministic authority receipt `8b374731...` over the current MKIV rules, Steamroller 2026, card data, official inventory, interaction graph and legacy audit; official/core/card/Steamroller tampering invalidates it. Current status is intentionally `rules_semantics_unreviewed`: `16` fail-closed reasons quarantine seven claim classes, Search's `77`-file execution receipt is non-current, old checkpoint/calibration/training claims are false, Root/Layer3 quarantine all ten historical skill manifests and route3 launch is false. Research execution may continue without Strict/training promotion.
- [x] Complete Ticket 23 Slice 23.1. Search now consumes the Engine-owned automatic hit/miss outcome instead of owning a duplicate priority rule: automatic miss and automatic hit each produce one deterministic attack outcome, the explicit roll branch produces 36 outcomes, and probability mass is conserved. Real Engine parity proves Witch Mark + Stealth misses, True Sight restores Witch Mark automatic hit, the roll choice is LLM-visible, and Spell Ward rejection remains replay-visible. Shared-source recertification receipt is `eefdb6c5...174d8`; this bounded certification cannot promote `Q_rule`, skill, values or online experiments.
- [x] Complete Ticket 23 Slice 23.2. Search observes role-based round changes and complete attack lifecycle events from the Engine Host; it does not own turn or lifecycle primitives. A reversed-first-player turn advances round 2 to 3 correctly, and a lethal Free Strike arrives as damage -> disabled -> boxed -> destroyed -> attack_resolved with a validated receipt. The aggregate also replays the affected duration/lifecycle consumers and micro `108/108`; source receipt is `5380afee...3426`.
- [x] Complete Ticket 23 Slice 23.3. Search imports the Engine Host and never implements the six Unit semantics locally. Two real-source Great Bears each consume an initial Great Axe attack, expose independent Rapid Strike additional slots and combine those two exact slots into one Layer3-RNG-backed CMA; the Host consumes both once and closes the Unit window. Aggregate source receipt is `1ab1df16...8481`.
- [x] Complete Ticket 23 Slice 23.4. Search now binds reviewed Engine receipt `03087012...c988` (`288/288`, `679` execution-source files) and owns no duplicate geometry rule. Host authority plus 12 geometry consumers verify obstacle/path classes, path topology, predicate plans, movement-event automata/languages, cell partition/connectivity, strict representatives, selected-Unit movement witnesses and activation order. Unsupported continuous unions remain unresolved rather than being sampled legal or unreachable.
- [x] Reassert the repository boundary before continuing Slice 23.5. Search and Engine remain independent Git repositories with independent GitHub remotes; the parent workspace ignores both nested roots, and the Chance parity verifier now loads the selected receipt-bound Engine through the shared Host loader instead of a hard-coded sibling source import.
- [x] Complete the bounded Ticket 23 Slice 23.5 core. Engine certifies `29` attack/damage semantics, `32` primitives, `90` literal oracle fixtures, `36` executor-evidence cases and `97` mutation obligations. Search Host parity conserves `12` primary Chance classes, the defender's `2` damage-transfer choices and `6` post-transfer life-spiral classes, with no Search-owned rules implementation; current report `555d460b...f5f5` binds Host `9bcbd810...202b`. Dragoon excess damage and arbitrary legal dismount placement now survive sealed replay. This does not certify current card-specific attack templates, continuous geometry or full Root/App/Layer3/Search publication parity.
- [x] Start Slice 23.6 with a duplicate-safe historical inventory rather than rewriting the existing resource/spell executors. Fifteen Engine rule/harness-focused checks pass; one real Layer3 bridge defect was fixed so explicit token/card ARC reaches the Engine and a Focus-5 ARC-7 warcaster replenishes to 7 after a full opposing turn. Two other failures were stale verifier fixtures, not executor defects. The browser-only spell-stat display check remains outside this bounded rule audit because Playwright Chromium is unavailable.
- [x] Reclose the Search probability/recovery regression after freezing both repositories on current Host `9bcbd810...202b`. The full gate passes with report `f4634cec...2603`: `1,927` nodes, `2,674` edges, `68` Chance audits, `816` owned-response audits, exact value `1225/1296`, conserved threshold ledgers, external-DAG restore/stitch and sequential/parallel continuation equivalence. The earlier report-hash failure was receipt drift caused by editing Engine source while parent and child processes were alive, not lost branches or an algorithm defect.
- [x] Complete bounded Slice 23.6a for ordinary Maintenance and Focus. Engine binds five official-source semantics to six shared primitives, `22` literal fixtures and `12/12` killed mutations; the old control-cycle verifier passes `42` assertions, Layer3 passes `200` ARC/target-token checks and micro battles pass `108/108`. The re-audit found and fixed a real old defect: warjack Focus could follow caller `resourceMax=4`; Power Up and allocation now share the official fixed cap of three and preserve their primitive result in replay. Receipt is `7b6b5d06...c934`; parent 23.6 and Ticket 23 remain incomplete.
- [x] Complete bounded Slice 23.6b for ordinary core Fury by recertifying the historical executors. Engine binds eight semantics to nine primitives, `35` literal fixtures and `13/13` killed mutations; historical leech/Spirit Bond/forcing/threshold/frenzy/transfer/reave/heal/aspect focused paths and micro `108/108` pass under `47e7422b...ef5e` / authority `54ab5d64...e394`. Real repairs cover current ARC rather than declared max, exact 2d6 threshold input, ordinary Construct healing rejection, selected transfer-recipient Chance and Wild non-force Fury gain. Provoke, Elemental Mastery and special reavers remain named card-interaction debts rather than hidden completion claims.
- [x] Complete bounded Slice 23.6c for ordinary spell/upkeep/animus/feat/channeling semantics. Engine certifies `7` source-bound semantics, `8` shared primitives, `26` literal oracle fixtures and `9/9` killed mutations; core executor `15`, command/channel `36`, card-text `172` and micro `108/108` pass under Engine receipt `f7f7ab87...a2f4` and authority `54ab5d64...e394`. Search imports the same hashed Engine rules/primitives/Layer3 sources and observes fixed-cost upkeep, own/borrowed animus, out-of-control rejection and once-per-activation behavior under Host `860839a1...a931` / parity report `dda66ab1...e182`, with zero Search-owned spell rules. Parent 23.6, Essence/special resources, card-specific replacements, complete action-space search and all promotion gates remain open.
- [x] Complete Slice 23.6d and parent Slice 23.6 Host parity. Engine 23.6d certifies `13` Essence/special-resource semantics, `13` primitives, `30` fixtures, `16/16` mutations, `9/9` focused Engine/Layer3 cases and micro `108/108`; Search binds the exact Engine rules/Essence primitive/adapter sources and preserves eight representative legal/rejected/transition/Layer3 cases under report `a980d3c6...252a`. Parent 23.6 then binds current 23.6a-d receipts, `33` semantics, `36` primitives, `113` fixtures and `50/50` mutations plus nine Engine interaction cases; Search Host `98ba424d...d6af` replays seven Focus/Fury/Essence spell/allocation/force/identity-isolation cases with zero Search-owned resource rules under report `756e25be...7e6d`. Card-specific replacements remain Slice 23.8 debt and no global/training/value claim is promoted.
- [x] Complete Slice 23.7a Host parity for ordinary force construction and runtime identity. Search delegates physical-model composition and encounter-level legality to the exact shared Root/Engine Force Builder, with no local model-count parser or final construction rule. Real Sepsira/Fane/modular/all-army roster probes plus Character, FA, attachment, battlegroup, loadout and Companion evidence bind the Engine's `30` fixtures and `18/18` mutations under current parity report `3e63e134...bb35`.
- [x] Complete Slice 23.7b Host parity for core setup and deployment. Search binds the exact Engine deployment primitive/rules hashes and executes the same starting roll, first-player/edge choices, ordered deployment, post-deployment Prey and ARC resource initialization through `warmachineHost`; strict protocol bypass is replay-visible as rejected. Search owns zero final deployment-legality rules and may only propose/order continuous placements. Current parity report is `83db38bf...ea9`.
- [x] Complete Slice 23.7c and parent 23.7 Host parity. Search binds current Host `ec575f86...16c0`, executes scoring-open/closed windows, duplicate-ledger rejection, exact scenario setup, Wolves short-move rejection, Payload contact stop and High Stakes missing-Chance rejection, then replays one continuous force-entry -> scenario-setup -> first-deployment chain. Parent totals are `36` semantics, `23` primitives, `109` fixtures, `67/67` mutations and micro `108/108`; Search owns zero final lifecycle legality under report `e4f8200a...8f33`. Ticket 23 is `8/12`; values, skills, training and online remain false.
- [x] Record the Engine-only Slice 23.8a-d boundary before Search parity. The current dynamic denominator is `3128` card sources with `409` exact proof-closed, `137` declared proof-pending, `338` contract-mismatch and `2244` unowned after exact-text reuse, structured closure of all `474/474` proof roles and parameterized Shadow Shift placement closure. Engine reports are 23.8a `cdad6142...01f9`, 23.8b `e081a69f...98b7`, 23.8c `a4daf987...fb2b` and 23.8d `f44e35ee...7aa9`; all `385/385` registered atoms are proof-closed. Search has not yet exposed the new Shadow Shift parameterized Host helper or promoted any card-source legality, value or pruning result; parent 23.8 Host parity remains pending and all downstream gates remain false.
- [x] Bind the bounded Engine Slice 23.8w exact-reuse batch through the shared Search Host. Search executes exact True Sight through cloud/Stealth, exact Guided automatic hit, and exact Heavy Weapon Hardpoint eligibility inside the Weapon Crate contact window without implementing any of those rules locally. The focused parity report binds Host receipt `8f255f...7fa6`, reports zero Search-side rule implementations and contributes to Engine aggregate `16a9c26f...355a`; Engine closure is `554` exact / `2605` unresolved with micro `108/108`. This is a bounded Host-consumption proof only: parent 23.8, global Strict, `Q_rule`, value, Skill/training and online gates remain false.
- [x] Bind Engine Slice 23.8x's 18-source Field Marshal grant batch through the shared Search Host. Search enumerates the Engine registry rather than owning a Field Marshal table, observes all `18` exact grant materializations, executes transported True Sight through cloud/Stealth and transported Unyielding as exactly `+2 ARM` against melee damage, and contains zero matching source IDs or grant-atom implementations in `src/`. The focused parity report binds Host receipt `00fdc66c...27f`; Engine aggregate `1ae7723b...a085` closes `572` exact sources and leaves `2587` unresolved with micro `108/108`. This does not certify the remaining `40` Field Marshal sources or promote parent 23.8, global Strict, `Q_rule`, values, Skill/training or online experiments.
- [x] Reissue affected local Search Host parity after the Engine Focus/Fury/spell source changes. Current Host `860839a1...a931` preserves all 29 Slice 23.5 semantics, `12` primary Chance classes, `2` defender responses and `6` post-transfer life-spiral classes with zero Search-side rule implementation; current attack/damage parity report is `96243cdf...d37c` and the Slice 23.6c spell report is `dda66ab1...e182`. The full `f4634cec...2603` probability/recovery report remains useful provenance but is not current for this new Engine receipt; rerun that long gate once parent 23.6 freezes instead of after each sub-slice.
- [ ] Complete Slices 23.8-23.11 and the dynamic completion report before promoting Ticket 20 `Q_rule`, Ticket 22 real strategy binding, Ticket 21 values, skill, reports or online experiments. Whole-Unit opponent-response/Chance partition and full Search cross-runtime parity remain later closure, not bounded executor claims.

## 2026-08-26 random ROF Chance integration

- [x] Treat Engine `declare_random_rof_ranged_attacks` rows as Chance nodes rather than player choices. Search validates every Engine-owned weapon-instance factor, expands their exact Cartesian support, carries rational numerator/denominator mass and applies each outcome through the same strict Host before target or CRA selection.
- [x] Add current-data Kithguard execution evidence. One Slugger `d3` produces three accepted strict successor classes with mass `3/3`; a synthetic two-weapon `d3+1` declaration produces nine classes with mass `9/9`. The policy-step, basic Chance parity, probability DAG, adversarial-min regression, Unit slot domain and complete activation shell remain green.
- [x] Preserve the Engine's unresolved-random-ROF debt in Search's Unit attack-slot domain. A pre-roll Kithguard plan can no longer claim its printed slot denominator is complete; after the exact chance edge resolves, the denominator becomes complete for that state.
- [ ] Extend this local Chance closure through the complete ordered Unit Combat Action and opponent-response fixed point. Exact random ROF does not by itself prove every subsequent individual/CRA grouping, target, attack roll/damage outcome, response or successor class.

## 2026-08-25 Goal-conditioned strategy quotient

- [x] Split the completion problem into a rules-behavior quotient `Q_rule`, a receipt-bound goal strategy quotient `Q_goal`, and downstream multi-turn adversarial evaluation. Ticket 20 remains responsible only for complete Host-observable action/geometry behavior; new Ticket 22 owns strategy preservation, and Ticket 21 now depends on both.
- [x] Research value/policy-preserving abstraction, probabilistic alternating simulation/bisimulation, exact Pareto limits, CEGAR, optimistic bounds, symmetry/POR, symbolic storage, interval abstraction, continuous motion planning and mature product practice. The academic/product v2 reports cover `31` primary research sources plus PRISM-games, UPPAAL, Storm, Gurobi/CPLEX/SCIP, poker/chess solvers and OMPL/SBPL/Nav2. They establish that reverse search must over-approximate all potentially effective strategies; permissive strategies precede optimization; hard pruning needs a valid infeasibility, alternating-dominance or admissible-bound certificate; and product tree quality never proves omitted Warmachine actions irrelevant.
- [x] Implement the finite `warmachine_goal_conditioned_strategy_quotient_v1` prototype. It computes one-way goal-compatible alternating simulation for dominance, a separate symmetric probabilistic alternating bisimulation for quotient merging, exact rational distribution lifting, complete forward reachability, componentwise optimistic-upper versus concrete-policy guarantee-lower pruning, and a conserved disposition/complement ledger.
- [x] Verify the prototype on a 17-state, 12-candidate adversarial stochastic game. Different coordinates with identical behavior merge; a detour, an opponent-harmful state and a worse Chance state have valid one-way witnesses; equal-mean/different-distribution Chance states and different opponent-response states do not merge; a reverse-omitted superior branch enters the forward complement queue; an inverted opponent prune fails closed. Closing all dispositions yields `6` retained, `4` dominance-pruned, `1` unreachable and `1` upper-bound-pruned cell with zero unresolved.
- [x] Run the three-method experiment before changing the roadmap. On a fixed `66`-candidate / `11`-behavior-class / `6`-effective-class game, complete forward requires `58` expensive strict roots, pure reverse processes `65` discovered roots yet misses the hidden effective class, and the quotient hybrid closes `6/6` with `9` roots, zero unsafe hard prune and one complement challenge (`84.48%` fewer roots than complete forward). Across `128` same-budget deterministic permutations, forward/reverse mean recall is `13.67%/9.24%`, neither reaches full recall. The first failed run also established that initial-distribution/history/reachability context must participate in the strategy comparison signature; an unreachable future-equivalent state cannot represent a reachable strategy.
- [ ] Bind Ticket 22 to Ticket 20's real receipt-bearing Warmachine `Q_rule`, terminal/objective/strategy/opponent/Chance closure receipts and strict lower witnesses. Until then `effectiveStrategyQuotientComplete` for the real custom matchup remains false.
- [ ] Add strategy quotient classes, Pareto lower frontier, complement optimistic upper set/gap and every prune certificate to Ticket 09/10 reporting before any “all effective strategy classes” product claim.

## 2026-08-25 Ticket 07 current-Host reclosure

- [x] Reclose the expanded-budget Hysene score route under current Host `c812a00b...`. One legal `113`-model deployment reaches the terminal through `96` reverse edges and independently strict-replays with oracle isolation; `111` rejects and `1011` unresolved branches remain explicit. Report v3 hash after a same-contract confirming repeat is `5276b2b8...`, semantic hash `cc242803...`, and opening state hash `a7e2ee89...`.
- [x] Separate resume compatibility from deterministic report identity. Larger budgets may continue a valid checkpoint, but `warmachine_search_run_contract_v1` now distinguishes result-affecting options while ignoring callbacks/checkpoint stores. The focused verifier proves option-order/runtime-cache invariance and budget separation; the real Hysene same-contract repeat sets both comparable/equivalent flags true and preserves every route, ledger and replay hash.
- [x] Reclose the Hysene score root from terminal history to one legal deployment under current Host receipt `3bf551f7668136d4b659b2f0c5c987bb7cb7fa9aa0dd0641276d654f14e10848`. The selected route has `96` reverse edges, one independently strict-replayed opening `5c32dbc81c065e1d95fd812d62fe61777139d0c73ffadd2e0f5622507347ef41`, report hash `b8f58c77e1551502a81cd3d7ede1f56f66acbb21ccb5a6dc7b3a847cf722cf42`, `8` strict rejects and `1042` unresolved branches. Declared movement history now wins candidate ordering without deleting alternate legal origins.
- [x] Rebind Ticket 19 and Ticket 08 to the fresh route. Ticket 19 independently certifies five score-settlement windows and seven sources through final `6:1`, retains the source fixture's old Host receipt as provenance, and rejects a direct `4:0` rewrite; report hash is `93bce515dd0ce296c031c4f25966996adad364c7468a325bba00487e686b5ba9`. Ticket 08 source parity passes with value-set hash `b612f65b613c64b34127ae783e3c3fa061fbcd2064083059b18695c81ae7f0cf`; both real cells remain `[0,1]` because candidate, opponent-response and Chance closure are incomplete.
- [x] Reissue the five archived Ticket 09 route units under Host `c812a00b...`. Auricant/Hysene/Nymara assassination freshly execute `53/55/54` strict transitions. Ashmael and Auricant score each reissue `96` reverse edges through `112` current transitions, independently replay the rebuilt route and certify all score-history windows; route-unit hashes are `dc15a851...` and `21b329e2...`.
- [x] Repair archived whole-Unit movement evidence without weakening strict execution. A stale selected-model straight lane remains rejected; reissue may use a current-Host detour, or choose a different legal model as the same Unit movement anchor when the original must travel its full allowance through a sibling. Action/resource/Unit/endpoints stay fixed, every migrated edge must reproduce the exact source successor hash, and the complete rebuilt route independently replays. Positive path/anchor and forged-successor negative fixtures pass.
- [x] Discard and regenerate the unfinished Nymara score historical frontier instead of resuming its two blocking-terrain overlaps. Under Host `c812a00b...`, the replacement reaches one legal deployment through `96` reverse edges, independently strict-replays and passes oracle isolation with `15` rejects and `999` unresolved branches; report hash is `fd51e093...` and opening state hash is `3634c156...`.
- [ ] Reclose Ticket 17 recovery evidence after the activation source-closure change, then rebuild Ticket 09 values, route identities and the custom matchup evidence report from only current-Host route units.

## 2026-08-25 Complete action and game-tree denominator

- [x] Add Custom Matchup Tickets 20, 22 and 21 after the route-existence audit exposed the missing denominator. Ticket 20 owns Host-complete activation families and proof-carrying continuous geometry/path/Unit behavior cells; Ticket 22 owns the target-conditioned complete strategy quotient; Ticket 21 owns horizon-indexed reverse target domains, exact-opening strict forward domains, adaptive same-denominator direction scheduling, full opponent responses and conserved real Chance outcomes.
- [x] Research fixed-opening progression, goal-formula regression, symbolic forward/backward traversal, Hamilton-Jacobi reachable sets, continuous motion planning and front-to-front search from primary sources. The resulting `docs/research/forward-vs-backward-goal-conditioned-search-v1.md` selects reverse constraints plus strict forward construction as the default, while preserving per-layer empirical direction switching and explicit non-dominance boundaries.
- [x] Audit the reusable geometry/search components and publish `docs/continuous-action-domain-v1.md`. Existing symmetry/permutation quotients, terminal finite reduction, lazy discrete cursor, strict CEGAR and Host visibility paths remain inputs; the missing implementation is a source-bound predicate/event-surface plan, path-class closure, transition-stable partition refinement, factored Unit domain, combined activation cursor and completion-aware direction scheduler.
- [x] Complete Ticket 20 Slice A1 Host-to-Search predicate-plan binding. Engine now emits order-stable SHA-256 plans for board/base inset, rule-atom movement allowance, axis/circle/rotated terrain, model obstacles and rough/reaction/Unit debts; its focused verifier also found and fixed rotated terrain dimensions collapsing to zero. Search validates the Engine plan hash and primitive schema, preserves every wildcard debt, rejects tampering, assigns no Chance mass, and emits separate rule-behavior and bound strategy-context receipts. A history receipt change leaves `Q_rule` context unchanged but changes the strategy context, preventing an unreachable future-equivalent member from representing a reachable strategy. Finite cells, path classes and transition stability remain explicitly false.
- [x] Add the first conserved predicate-signature cell cursor. It lazily enumerates every Boolean combination of Host-declared endpoint event regions while requiring every legal endpoint predicate, supports deterministic pagination, assigns no Chance mass and rejects a tampered predicate-plan receipt. This closes only the declared Boolean-signature denominator; geometric nonemptiness, connected components, strict representatives, path classes and transition stability all remain false/unresolved. Promote `strategyContextKey` into the strategy-quotient compatibility relation and completion gate so missing or different initial/history/observation/reachability contexts cannot merge even when future transitions match.
- [x] Complete Ticket 20 Slice A2 endpoint classification and strict existence witnesses. Engine now emits a source-hashed endpoint evaluation for every declared predicate/event primitive; Search uses bounded critical-point probes only to discover candidates, then requires a matching Host signature, Host path proposal, exact strict action enumeration and accepted transition. A reachable two-signature rough-terrain fixture yields `2/2` witnesses; moving the event region outside movement allowance yields `1` witness plus `1` unresolved signature. Candidate failure never proves emptiness/unreachability and continuous choices still carry no Chance mass. The regression pass also corrected one stale Unit micro fixture whose selected model path crossed a sibling's old base; Engine micro coverage is restored to `108/108`.
- [x] Complete Ticket 20 Slice A3 exact path-signature preservation. Engine now classifies one exact quantized path with endpoint signature, exact geometry hash, segmented cost, ordered terrain-event trace, first blocker, out-of-board and collision evidence. Search accepts caller path probes only after this Host receipt, strict enumeration and exact landing execution, and retains multiple unique path signatures in one endpoint cell. Focused evidence executes both a through-rough and around-rough route to `(14,10)`; they share one endpoint signature but retain two path/event signatures. Exact paths prevent unsafe merge, while homotopy denominator, trigger automaton and transition stability intentionally remain incomplete and zero Chance mass is assigned.
- [x] Add Ticket 20's first transition-refinement counterexample. Strict geometry witnesses now bind the successor semantic state and ordered Host event sequence; the same `(14,10)` endpoint has two accepted paths but two transition-observation signatures, so the containing Boolean cell is explicitly marked for refinement. Different observed signatures prove instability, while equal finite observations still cannot prove equivalence. The shared semantic state hash was extracted without changing existing callers; terminal-event, movement-predecessor, reverse-state and history-probability regressions pass.
- [x] Materialize the first recoverable CEGAR refinement ledger. It groups all executed witnesses in a parent Boolean cell by transition observation (`6` classes in the focused outside-rough cell, including the two same-endpoint path classes), emits the observed event/state refinement axes, and conserves an explicit unresolved continuous remainder. These finite observed groups do not claim geometric regions, exhaustive path classes, Chance mass or transition stability.
- [x] Add the recoverable Ticket 20 discrete activation shell. `complete-activation-domain-v2` seals every current Host activation-group by action-family slot, treats mandatory Control as one global window, partitions one unscoped Host enumeration with a focused Host-scope-parity classifier, assigns each accepted/rejected row exactly once, supports tamper-checked completed-page resume, and preserves all continuous choices as non-Chance debt. Five micro states have exact full-Host parity. The real 106-model Sepsira/Fane opening closes `102/102` group-family slots with `577` accepted and `7341` rejected rows in `507364 ms`; full-state parity was deliberately deferred (`null`), `33` continuous debts remain, and activation completion stays false. Reproduction and boundary: `docs/research/complete-activation-domain-real-opening-canary-v1.md`.
- [x] Extend the theorem-backed endpoint component-count certificate. A strict witness proves nonemptiness; finite convex intersections certify open space `1/1`. Strictly interior convex exclusions classify pairs and nerve triangles, compute exact first homology over `GF(2)`, then use Alexander duality for complement component count. Rough inside/outside `2/2`, isolated/overlapping obstacles and a triple-filled cycle have one component. A three-circle ring proves exact `H1=1` and two endpoint-formula components; those components are not yet separately materialized. Inflated touching and boundary-spanning cases remain unresolved. A partial page cannot claim the full denominator, and strict reachability/path/transition stability remain open.
- [x] Add the first complete path-topology subcase. In the exact Host-declared open convex advance domain, every fixed-endpoint legal path shares one relative homotopy class and the straight segment is canonical. Rough terrain, obstacles, Unit wildcard geometry and receipt tampering fail closed. Strict universal action execution and transition stability remain false. Adding this entry increases the Search execution closure to `72` files. Ticket 20 remaining order is now explicit: general endpoint decomposition, obstacle/event path classes, Engine-owned parameterized strict legality, Unit geometry, non-movement parameters/activation binding, then alternating transition fixed point before Ticket 22 and 21.
- [x] Add the first source-bound hazard path-event surface. Same-endpoint acid crossing and avoidance routes both strict-execute, but only crossing applies corrosion, so the rule-behavior observations remain two classes. Engine now declares the actor-base/trigger-range-inflated hazard region, ordered entry evidence for axis/circle hazards and a mandatory path-partition debt; rotated entry ordering remains pending. The rough-terrain control still merges audit-only differences under the narrow basic-advance behavior projection. Engine focused/endpoint/micro `108/108`, Search representative/lazy and the `72`-file execution closure pass.
- [x] Refine hazard geometry and ordering to exact Engine parity. Rectangular hazard regions now use the rounded-rectangle Minkowski sum for actor base plus trigger range, including rotation. Opposite fire/acid routes preserve `fire→corrosion` versus `corrosion→fire` in both Host path receipts and strict transition events; rotated acid entry also matches execution. The complete event language/reaction automaton remains open.
- [x] Materialize the narrow two-component three-circle ring. Exact nerve `H1=1` gives two components; the circle-center Jordan triangle is covered by the forbidden union, the outer component binds a strict path witness, and the center component binds a formula-valid point proven unreachable from the outer source. General mixed/multi-hole component materialization remains open, so the global legal-reachable geometry gate stays false.
- [x] Add the first Engine-owned parameterized strict movement action contract and migrate Search to it. Search submits one cent-inch-quantized finite advance/run path; Engine alone injects its internal path, evaluates, strictly enumerates and returns a hash-bound receipt/runtime action. Arbitrary partial advance/run pass, while blocked, invalid and Unit-joint paths fail closed. This removes a Harness/Engine seam mismatch but remains one-action evidence, not universal continuous-domain legality.
- [x] Close the first universal strict continuous-parameter subdomain. Search now binds the Engine theorem/receipt proving that, for one live non-Unit model in the exact empty open-convex domain, every `0.01"`-quantized endpoint satisfying board inset plus allowance has one canonical straight advance/run that strictly enumerates and applies. Host contract hash, actor/action/predicate binding and tamper rejection pass; rough/event, obstacle, Unit, rule/status/scenario and activated-state cases fail closed. The refreshed Engine matrix passes `268/268` under one `633`-file closure. This closes only the open-board single-model action-execution subdomain: general paths, events, Unit geometry, non-movement parameters, transition stability and `Q_rule` remain open.
- [x] Close the first complete obstacle path-class subdomain. For one axis-aligned rectangular configuration obstacle strictly inside the convex board/allowance domain, with a fixed endpoint antipodal to the actor and Host allowance below `3π` times the obstacle inradius, angular-lift classes prove that every legal path is exactly one of the clockwise/counterclockwise half-turn classes. Both classes have independent quantized Engine strict witnesses; direct crossing rejects, while longer allowance, non-antipodal endpoints, event surfaces, multiple obstacles and tampered receipts fail closed. Horizontal, vertical and run cases pass; the fixed endpoint has `2/2` classes and the next-class lower bound is `18.8496"` versus `12"`. This does not close arbitrary endpoints, multiple winding classes, general obstacles or transition stability. Search execution closure is now `73` files.
- [x] Close the first finite movement-event automaton subdomain. Engine emits exact ordered enter/exit boundary traces for supported convex rough/hazard regions, rejects endpoint-boundary and unsupported/tangent ambiguity, and proves a source-bound finite upper state count from the `0.01"` positive-segment bound. Search replays the membership automaton and rejects forged traces. The original finite-state slice deliberately retained simultaneous-effect debt; the follow-up below closes the supported pure-status case without upgrading the event-word language or transition stability.
- [x] Correct geometric hazard resolution and close the supplied pure-status simultaneous subgroup. Engine strict execution now follows exact entry parameter rather than terrain-array order, collects hazard damage requirements on every explicit path segment, and applies one area's entering effect once per advance. Simultaneous fire/corrosion status additions execute only under a Host-signed commutative/idempotent semantic-successor confluence proof; no rules order is invented. Simultaneous damage/removal/reaction/unsupported groups fail closed. Search validates nested effect receipts, rejects forged confluence, exposes supplied-path declared-effect closure in the main topology receipt, and keeps global `rulesEffectResolutionComplete=false` because realizable event-word, reaction/external-hook and transition closure remain open. Engine matrix `268/268` passes under stable `633` files at receipt `3ca4b0c5...`; Search production closure remains `74` files.
- [x] Close the first complete geometrically realizable event-word subdomain. Engine signs a conservative start-to-event-boundary distance lower bound; if every declared boundary is farther away than the full movement allowance, rectifiable-path length proves no legal movement path can cross one. Search requires that universal certificate plus an accepted zero-crossing Host trace, then materializes the exact one-word denominator `{epsilon}` and rolls it into the main topology receipt. A far acid pool passes with denominator `1`; a reachable pool and a missing strict witness remain incomplete. Reactions/external hooks, reachable-boundary languages and transition stability stay false, so Ticket 20 remains open. Engine receipt is `6accb763...` (`268/268`, `633` files); Search production closure is `75` files. Ruleset impact remains only `core_executor_changed` at `e8cda4ee...`; the candidate baseline is not promoted.
- [x] Close the quantized static movement event-word worklist and factor its selected-Unit-mover path domain. The resumable endpoint/path DAG scans every Host-admitted cent-inch endpoint and every nondominated quantized waypoint prefix, preserving event words and rough-region history; it can reconstruct the retained path representatives from a hash-bound receipt. Binding one exact Unit group discharges only that selected trooper's path debt. Other trooper placements, reactions/external hooks and successor equivalence remain separate and cannot inherit this completion claim.
- [x] Add the Engine-owned parameterized Unit advance/run action and bind Search to it. One request identifies any live selected trooper, its exact path and one exact destination for every other live Unit member; Engine validates the complete denominator, injects the Unit path only internally, enumerates the exact action and strictly accepts or rejects it. Zero-distance selected movement and stationary non-selected placements are supported. The refreshed eight-shard matrix passes `269/269` (`235` rule-atom plus `34` strict-transition) under `634` source files; source receipt is `269e2f2b...` and Search rejects source drift.
- [x] Close the first complete Unit-joint micro denominator for one action family. For one selected mover, Search combines every retained selected-path representative with the complete cent-inch board-point product for all other live members and delegates every tuple to the Engine action above; the micro ledger conserves `16 = 4 accepted + 12 rejected` across four resumable pages. A second aggregate requires exactly one complete receipt for every Host-declared live member and closes the fixture's `advance` selected-mover denominator. This is not full Unit movement: run, charge/failed charge, special movement, reactions and successor equivalence remain open.
- [x] Make opponent reactions visible as an independent `opponent AND` parameter domain. The v2 response projector preserves every Host-declared use/decline, finite destination probe and damage-transfer option; its resumable BigInt worklist strictly executes each ordinary option and keeps destination continuity, priority order, Chance and compound transfer as separate debts. A real Countercharge run exposes `9` declared options, exhausts them in three pages and strict-accepts `9/9`; a quiet action closes at `1/1`. The complete activation shell now emits per-slot opponent-response debt instead of hiding it inside transition stability. Countercharge's finite destination probes and deterministic dice sample do not close the response domain, so `opponentResponseDomainComplete=false` remains correct.
- [ ] Review and promote the new composite Search ruleset baseline after the required full fixed benchmarks and applicable calibration gates. The generated candidate reports only `core_executor_changed`; the old reviewed snapshot intentionally fails on the new Engine source/receipt fields until promotion, while upstream, geometry and search-execution receipt gates already pass.
- [ ] Complete Ticket 20. Remaining rule-domain work is explicit: close all Unit movement families and real Unit sizes; close the complete Unit Combat Action partition for combined melee/ranged attacks across primary, weapon profile, participant attack multiset, target, charge contribution and ordered multiple attacks; bind all other non-movement activation parameters; upgrade the new response worklist from declared finite probes to complete continuous destinations, all legal priority orders, compound damage transfer and exact Chance distributions; close external hooks and mandatory effects; prove successor-class stability under the resulting player-OR/opponent-AND transition relation; and import those receipts into the production activation planner. Any unresolved combined-attack partition or response axis keeps all applicable completeness gates and `Q_rule` false.
- [ ] Complete Ticket 22 against the real Ticket 20 behavior quotient, then implement Ticket 21 and compare pure fixed-start strict forward, pure symbolic reverse and reverse-domain-guided strict forward/bidirectional closure on the same assassination and conjunctive score benchmarks before selecting per-layer expansion. Upgrade the existing witness-cost scheduler with separate denominator-closure and value-interval-tightening objectives; a fast witness with high unresolved debt cannot win a completion batch.

## 2026-08-24 charge predecessor and opponent-response correction

- [x] Re-audit the Ticket 06 turn-one Vordak witness against the real persisted state. Vordak starts at `(17,5)` with `SPD 7`; the scripted run to `(17,17)` consumes the full `12\"` run allowance, while every declared charge is a failed charge capped at `10\"` because the nearest enemy center is about `32.8\"` away. At player1 turn-one start there are five legal failed-charge declarations against Vordak but zero immediate single-activation material target actions; after Sepsira's run there are four failed charges and again zero immediate material target actions. Alternate enabling activation sequences remain unresolved.
- [x] Add a current-Host Ticket 06 turn-one action-scope verifier. It proves `run=12\"`, `charge=10\"`, nearest enemy center `32.8\"`, zero successful Vordak charges, 24 failed-charge variants, five player1 failed charges before Sepsira runs, four afterward, and zero immediate material target actions in both recorded player1 states. Report hash is `7964689d24439628ad3d9c226802aba0eeb33382082f1e18dfe226592bb1ebda`; whole-turn enabling sequences and later-turn response closure remain false.
- [x] Implement and integrate the opt-in complete charge activation predecessor. The benchmark binder preserves target-bound `charge` paths; accepted successful independent and Unit candidates replay one exact straight segment, the mandatory charge attack and activation completion to the exact successor, while failed charge candidates replay the legal movement and immediate activation end without inventing an attack. The focused Unit route is `unit_group_charge -> unit_charge_melee_attack -> end_unit_trooper_combat_action`; automatic target-ray origins, explicit origins, exact action patches and over-distance fail-closed evidence pass.
- [ ] Replace the coincidentally correct hard-coded Ticket 06 route generator with candidate-complete movement/failed-charge/charge-combat selection. The reverse route now admits `advance`, `run`, successful charge-combat and failed-charge predecessors with every budget omission ledgered, but the historical forward witness still hard-codes Vordak's run and uses `completionOnly` for all unspecified activations; the widened real-route composition still requires revalidation.
- [ ] Expand opponent attacks/reactions as adversarial response sets for value work. Ticket 06 remains a valid existence witness, but its `completionOnly` turns do not close opponent choices; Ticket 08 must retain `[0,1]` until AND/min response and Chance mass are complete.

## 2026-08-24 Custom Matchup Ticket 09

- [x] Replace map-name-only representative openings with three content-bound exact terrain realizations and a fail-closed topology audit. Open/mixed/dense lane openness is `0.958333333 / 0.791666667 / 0.125`; mislabeled and out-of-bounds geometry is rejected, both initiative orientations bind the exact Two Fronts Scenario Terrain branch, and the audit explicitly does not claim a natural map distribution or continuous path reachability.
- [x] Rebuild the fixed Sepsira six-Swarm versus Fane opening denominator. All `288/288` selected openings pass exact Force Builder roster legality, deployment geometry, static placement and current-Host Steamroller binding with zero reject. Each of four Fane leaders has `72` strict openings: `24` on each required map, covering balanced/refused-flank/wide deployment, both initiative sides and four Cryx representative lists.
- [x] Generate deterministic evidence report v2 hash `bdf3ad1304286ed9ff55da678a57187af124ea20d566f38992c628be8ecc0eb5`. It separates legal roster evidence, ordering-only screening, topology-certified openings, strict routes and `[0,1]` value closure; map-opening debt is now `0`.
- [x] Bind route evidence by exact identity instead of model count. Ticket 08 now persists current-Host opening state/evidence hashes; report v2 requires the matching route/terminal label, complete opening address, sealed finite-pool membership, two exact 100-point ledgers, model-count conservation and Leader identity. Synthetic hash/roster/address tampering fails closed and the real gate passes `2/2`; both retained legal route pairs are explicitly outside the current representative opening set. Current report hash is `e3b0480133cd3f76fd834198bdbd6b90d1cb9935aedf4e620ce52271c6ce52d0`.
- [x] Close five of the six previously missing current-Host route cells: Ashmael score; Auricant assassination/score; Hysene assassination; Nymara assassination. Each binds its exact route roster/opening identity, independent strict replay, Ticket 19 history, and explicit probability/opponent debt.
- [x] Close the final missing Nymara score route cell from a regenerated legal frontier under current Host strict replay.
- [ ] Rebuild the current Ticket 07/08/19 baseline and the eight-cell report from the completed current route set.

## 2026-08-20 post-audit mainline recovery

- [x] Close Ticket 08 with exact-rational initial-state adversarial values. Chance success/failure/pruned/unresolved mass, opponent `AND/min`, subject `OR/max`, strict opening/replay/history closure and candidate completeness now produce per-cell intervals without treating frequency or screening score as probability. Focused evidence has `2` exact and `5` bounded cells, reuses one equivalent-state computation, rejects invalid mass, preserves incomplete-response bounds and keeps user-declared aggregation separate from natural-win-rate promotion. Ticket 06/07 real cells both remain `[0,1]`; the current real report hash is `4720e9778262fd1689e5e02384fae596b11bade185e31d19c61bc5e0227a75c7`, value-set hash `12e5041ce30d3202f9a8e9ebbca9f13b978c3bc0b6f4a87bc2ff06ad2d90270d`, and compact evidence can run without historical `.scratch` while checking exact source parity when those artifacts exist.
- [x] Close Ticket 19 with a finite history-bound terminal schema and current-Host strict settlement adapter. Seven Steamroller scenarios and Kill Box timing/distance exceptions pass focused positive/negative fixtures; Ticket 06 binds two `0:0` windows and Ticket 07 binds five windows with seven exact scoring sources along `0:0 -> 0:0 -> 0:0 -> 3:0 -> 6:1`. A premature assassination-history `4:0` is mathematically excluded, while a direct real-route successor rewrite to `4:0` fails strict state matching. Combined report hash is `35c030a7857a4a0f039236b238268d4b4841574acf26b40726f101e734f9f9ee` under Host receipt `539dabd1710cd151ddb8a85c1107c81982be202584f834393d82adc7b79faa44`.
- [x] Reject whole-branch merge of `manus/phase1-ruleset-ticket05`; continue from both repositories' `main` and retain the collaboration branch only as a donor/problem archive.
- [x] Expand the custom-matchup Wayfinder denominator from `13` to `19` tickets. Tickets 14-18 cover Engine focused-verifier closure, cross-repository receipt/clean-clone reproducibility, candidate completeness, replay-transition atomicity, and lossless geometry reduction; Ticket 19 owns history-bound terminal hypotheses so score ledgers cannot drift away from exact settlement geometry.
- [x] Complete Ticket 14 and maintain an actually executed all-shard Engine report. After the Ticket 05 combat-sequence repair, the refreshed eight-shard aggregate remains `263/263`: `235` rule-atom and `28` strict-transition verifiers passed with zero failed, missing, duplicate, foreign, timed-out or source-drifted rows under one stable `628`-file closure. Current reviewed source receipt `239d80fd55217b752e09c0970df5072400b91f0f3c4a14338f277f2911255962` binds manifest `47679e27a96061e4d691c5dbf53e5ebf4cb83183f8e650dc79c75ff0f7625a93` and aggregate `d4574e4374673f261a0490229219f4a815c1cee00ccb791683bc333a5499c951`.
- [x] Complete Ticket 15 and promote the reviewed Search ruleset snapshot. The path-independent Search materializer/planner/executor/recovery closure contains `51` files and is currently bound to the refreshed Ticket 16 Engine receipt; it has no LLM, Provider or skill-router ingress. Historical checkpoints fail closed, both terminal batch gates self-generate their inputs, and fixed benchmarks default to Engine receipt-bound `fixtures/ruleset-baseline` instead of historical `build` output. The six-verifier cross-repository clean baseline passes `6/6`; the full fixed benchmark also strict-certified a `103`-model opening, a `140`-transition score route and a `60`-transition assassination route.
- [x] Complete Ticket 18 lossless terminal-geometry reduction. Host-only necessary-condition exclusions now carry input/evidence hashes; equivalence requires the complete Engine-observed geometry relation rather than base size or symmetry. Three structurally different full-versus-folded fixtures preserve every strict disposition and semantic replay while reducing strict representatives `4→2`, `4→2` and `2→1`. Existence early-stop records every remainder as unenumerated with `[0,1]`; the current `59`-file Search execution closure and cross-repository clean baseline pass `6/6`.
- [x] Close Ticket 05 under its finite-budget contract with plan `764d6d64...` / checkpoint `005bb9c8...`. Current production completes `51/64`: `13` task-specific strict roots, `4` Host strict rejects, `28` exact proposal filters, `6` official-source unresolved results and zero invalid inputs; the remaining `13` unsupported assassination partitions stay `budget_deferred` and preserve the `[0,1]` upper bound. Score is `11/11`, simultaneous `16/16`, fixed-round `13/13`, assassination `11/24`; all `39/39` unique openings are strict and candidate mass is conserved.
- [x] Complete the first Ticket 16 candidate-ledger slice. Cursor advancement now requires one sealed outcome per consecutive slot; examined nonterminal, examined terminal, strict rejection, proven exclusion and unresolved mass are disjoint and conserved. The focused verifier proves that a legal nonterminal first slot cannot skip a later terminal slot, and preserves distinct identities for multiple melee weapons, ranged weapons, offensive spells and rule-granted attacks together with Stealth/True Sight and model/terrain LOS evidence.
- [x] Complete the Ticket 16 production candidate-materializer and Revive slices. Revive now gives the returned Grunt both one-turn forfeitures, expires them deterministically and permits a different eligible Grunt to lead a unit charge. The assassination materializer now binds the same composite execution receipt as the batch planner, loads a fixed ruleset room when historical build output is absent, declares all actor/profile slots before execution, lazily expands only the current slot's Host action lines, accepts resumable candidate progress and preserves unresolved mass. A clean 64-task batch produced 39/39 strict unique openings; the 78-model Vordak/Talon route dealt 8/8/8, resolved the Feast decline, reached disabled/boxed/destroyed, matched independent replay and left the production checkpoint unchanged. The lazy plan reduced the observed two-task gate from over 10 minutes without reaching completion to about 2 minutes.
- [x] Resolve post-audit Ticket 17 transition recovery atomicity. Failed Host transitions can no longer enter the committed receipt prefix, active candidate slots do not advance before primary/replay completion, and batch tasks checkpoint sealed materializer progress without becoming completed. Three SIGKILL points, stale CAS, concurrent writer and unpublished-file fixtures pass. The real 78-model Vordak/Talon route generated nine intermediate checkpoints over five primary plus five replay transitions and exactly matched one-shot report `d5a12478...`; production v6 closed the task as the third strict root with `43/64` completed and `21` explicit budget-deferred.

## 2026-08-18 Team handoff repositories

- [x] Split the current Warmachine execution snapshot into a dedicated `warmachine-strict-engine` repository boundary without media, build outputs, Provider records, or credentials.
- [x] Make reverse search resolve a sibling Engine through `WARMACHINE_ENGINE_ROOT`, retaining `WARMACHINE_PROJECT_D_ROOT` only as a compatibility alias.
- [x] Centralize Force Builder, card-data, and optional media path resolution through the Engine Host root.
- [x] Add code navigation, architecture/design, exact handoff status, team workflow, and the full Ticket 05-13 development plan.
- [x] Replace the historical Gorman/Ticket 05 baseline with the current receipt-bound four-family batch and focused aggregate evidence.
- [ ] Complete Wayfinder Tickets 05-13 according to `docs/DEVELOPMENT_PLAN.md`.

## Repository Extraction

- [x] Create an independent repository boundary for Warmachine reverse search.
- [x] Define a narrow Project D Warmachine host adapter with source-bound dependency receipts.
- [x] Preserve a migration-only bridge to the existing reverse-search modules.
- [ ] Migrate pure reverse modules from `project-d/scripts` into this repository in dependency order.
- [x] Migrate the pure spatial quotient module with seven new/legacy parity fixtures.
- [x] Migrate the atom-backed reverse primitive contract registry with full-registry parity.
- [x] Migrate the rule-atom terminal regression engine with every existing fixture checked against legacy output.
- [x] Migrate goal-conditioned terminal generation and strict local validation behind the Warmachine Host.
- [x] Migrate the OR/AND/chance symbolic reverse worklist with existing-fixture parity.
- [x] Migrate formation/deployment proposal generation behind a lazy construction Host.
- [x] Migrate four pure search leaves: dependency footprints, terminal obligations, CEGAR, and roster investment.
- [x] Migrate three read-only Host leaves: chance classes, lazy action cursors, and terminal relevance closure.
- [x] Migrate strict rejection replay and bounded matchup search behind the Warmachine Host.
- [ ] Replace all migrated modules' direct relative rules imports with the host contract.
- [ ] Move focused reverse verifiers and make them pass from this repository.
- [ ] Remove the legacy reverse-module bridge after source and verifier parity.

## Wayfinder V2

- [x] Restart the project from the `/wayfinder` destination interview instead of inheriting the legacy implementation map.
- [x] Define the destination as an executable Steamroller reverse-search project that returns strict reachability candidates; strategy comparison is a separate downstream layer and never a faction-global optimum proof.
- [x] Create and evolve the canonical V2 map with eighteen decision, research, prototype, and task tickets; verify its dependency frontier with no missing or cyclic dependencies.
- [x] Preserve the resolved `18/18` reverse-search research map and create a separate downstream custom-matchup Wayfinder map. The new map has `13` vertical tickets: `1` resolved goal-conditioned roster-routing baseline and `12` open implementation/verification tickets covering generic task authoring, real task-roster terminal roots, Host rule-gap feedback, batched terminal roots, assassination/score reverse closure, adversarial probability values, the fixed Sepsira/Fane report, an interactive console, all-faction mechanisms, incremental updates, and final product gates.
- [x] Close downstream custom-matchup Ticket 02 with the first task-bound real-roster terminal root. Current-task/current-Host artifact coherence is fail closed; the exact `100/100`, `78`-model Sepsira/Hysene Two Fronts round-2 assassination independently strict-replays, while deployment reachability remains `0`, two representatives remain unresolved, the value interval stays `[0,1]`, and no win-rate/training claim is emitted. The work also corrected Warjack/Warbeast damage rows being miscounted as physical models in the generic roster pool.
- [x] Close downstream custom-matchup Ticket 04 with a source/Host/atom/interaction-bound rule-gap feedback gate. The real Vordak `Rampant Fury` + `Feast [2]` fixture exposed and repaired phase-only activation detection during Control-Phase Frenzy; the old `rules_unknown` action now strict-executes, source drift remains `strict_rejected`, four stale dependent artifacts invalidate, and Ticket 02 evidence has been rebuilt under Host receipt `ded3332139...`.
- [x] Close downstream custom-matchup Ticket 03 with a generic current-data task compiler and exact Force Builder finite universe. The schema covers arbitrary faction/army/leader modes, fixed cores, exclusions, per-copy loadouts, attachments, battlegroups, fixed-complete rosters, scenario/map/initiative/round and budgets; every rejected, omitted, deduplicated and deferred construction mass is audited. The fixed production task yields `27` Sepsira-core and `56` Fane legal candidates over `18,517` representatives/`2,049` demand groups, while focused Cryx/Fane, Winter Korps modular, Dusk all-army, fixed-complete and attachment-limit fixtures pass under construction receipt `cb49be72...`.
- [x] Close downstream custom-matchup Ticket 05: the corrected finite production plan schedules `64` terminal tasks across all four terminal families and `13` construction macro profiles from `10,628,352` candidate variants; all `64/64` tasks have current-Host strict openings, deduplicated to `39` exact states across all seven scenarios. Production completes `51/64` with `13` strict roots, `4` Host rejects, `28` proposal filters, `6` source-unknown results and zero invalid inputs; `13` unsupported assassination partitions remain explicit budget-deferred mass. The fatal-action LOS, all-initial-weapon sequence, boosted purchase resource cost and independent replay gates now pass.
- [x] Fix two Ticket 05 opening identity defects before terminal execution: deployment zones are oriented by Steamroller Attacker/Defender roles when player2 wins initiative, and the terminal-batch opening key now includes scenario and Scenario Terrain identity. The prior `39` scenario states shared only `9` construction-level opening keys; the rebuilt batch has `39/39` unique terminal opening keys while retaining each legacy construction key as provenance. Every task ledger row now resolves to the exact state/scenario/rosters and all Host placement, unit-formation and terrain audits pass.
- [x] Resolve the search/scoring and latest Steamroller terminal studies from primary sources; close both research tickets after the delegated agents stalled without output.
- [x] Define the complete rules-relevant state, victory-relative/absolute timing, and convergent-history transposition contract, including reverse-branch convergence with separate strict-certified proof suffixes.
- [x] Research and demo search-explosion storage: content-addressed state DAG, route-label preservation, external-memory duplicate detection, compression, concurrent commits, garbage collection, and crash recovery.
- [x] Keep every implementation, prototype, and grilling ticket blocked until all currently specifiable research tickets are resolved.
- [x] Implement the content-addressed external DAG, immutable segments, exact DDD merge, independent route/probability ledgers, transactional checkpoints, and pinned-root garbage collection before multi-layer search.
- [x] Align the Project D Host with Steamroller 2026 terminal semantics before the typed graph or terminal predecessor prototype can certify scenario routes.
- [x] Work through one named V2 frontier ticket at a time until the fixed Cryx/Fane dual-terminal benchmark, reporting/training contract, recovery protocol, and completion gates are all resolved.
- [x] After the fixed Ticket 09 benchmark closes, implement cross-turn recursive reverse search, a finite parameterized Steamroller terminal corpus, and cross-scenario/roster validation before claiming the project complete.
- [x] Add an exact-rational probability DAG for ticket 09: every Chance edge records conditional and contributed cumulative mass, equivalent same-depth states merge before threshold pruning, low-probability nodes remain stored, and success/failure/pruned/unresolved mass must conserve exactly to one. A real rules-v1 ranged attack executes all 67 exact dice classes with strict receipts and yields exact success `1225/1296`, failure `71/1296`, and zero rejected chance edges.
- [x] Add a multi-action stochastic-game evaluator that sums exact Chance classes, applies defender-owned choices as adversarial AND/min, memoizes equal state/cursor pairs, retains strict receipts on every executed response edge, and fails closed on nonzero probability thresholds until adversarial-context-aware same-depth merging exists. A real two-attack damage-transfer fixture proves exact success `1225/1296` across 13 evaluated states and 156 response nodes.
- [x] Attach that evaluator to a strict fixed-assassination checkpoint after 54 deterministic deployment/movement/control/activation transitions. The next action is Sepsira's boosted channeled Excarnate through the Raptor. All 17 dice classes conserve exact mass `46656/46656`, all defender responses are present, and zero strict edges reject.
- [x] Add a narrow state-bound proof for boxed-only successor effects: when the strict damage distribution maximum is below the target's remaining boxes, the `model_boxed` hook cannot fire and may not block exact Chance expansion. The fixed Excarnate maximum is 11 against Nymara's 24 boxes; a 10-box counterfixture remains fail-closed.
- [x] Add strict deterministic policy edges for movement/timing/special actions with receipts, attack-resolution rejection and reaction-requirement rejection; a real `pass` action proves the positive path.
- [x] Merge exact Chance classes only after executing and comparing their complete owner-aware response vectors. Positive/negative fixtures prove that transfer successors or terminal classifications prevent merging, while equivalent classes retain every original strict receipt. The two-attack fixture safely merges 110 classes across 13 Chance nodes; fixed Excarnate merges 5 of 17 classes into 12 groups with exact `46656/46656` mass and zero rejected responses.
- [ ] Keep the unfinished Excarnate/Sepsira/Raptor persisted policy only as an optional nonblocking forward oracle: its remaining deterministic caster actions, charge/additional attacks, transfer and terminal probability do not count toward Ticket 09 reverse-search completion.
- [x] Reclassify the existing fixed assassination and score routes as strict forward oracles only, and add a finite round-3 terminal-hypothesis generator whose typed assumptions and input firewall exclude openings, forward routes and intermediate states.
- [x] Implement the first terminal-rooted concrete reverse edge for assassination: from only the terminal state plus hypothesis cell, restore and strict-certify the exact enumeration-prepared state immediately before the killing attack. The hidden predecessor is recovered with 1 strict candidate, 0 rejects and 0 unresolved branches.
- [x] Add the scenario-score settlement inverse. A round-3 Pressure Point fixture reverses the `4:0` terminal into the exact `2:0` pre-settlement state and strict `end_turn` re-applies the 2-VP center objective, side switch and round increment with 1 candidate, 0 rejects and 0 unresolved branches; a discovered Kill Box false-positive was removed from the fixture.
- [x] Add strict inverse edges for pass activation, explicit-path movement activation, complete control phase and prior-player `end_turn`; compose them from each terminal root through one complete opponent turn without consuming hand-built intermediate states. Pass and movement remain peer OR predecessors, distinct activation-order labels survive state convergence, and deployment reachability has a separate full-state/construction/geometry audit.
- [x] Recursively compose the inverse edges until both explicit round-3 micro terminals reach legal turn-1 deployment. The assassination chain reverses four prior-turn boundaries and the scoring chain five; each currently returns one legal deployment witness while all budget omissions remain unresolved.
- [x] Blind strict-replay both complete micro reverse routes. Replay starts only from the recovered deployment, reverses the inverse-edge order, freshly enumerates every pass/movement/control/end-turn/terminal action, reuses only explicit path/choice/RNG witness payloads, and checks every intermediate semantic state hash; assassination and scoring are each certified `1/1`, with prior receipts excluded from execution.
- [x] Apply the terminal-rooted chain to finite round-3 Cryx/Fane assassination and score position batches, reverse at least one strict-materialized root per terminal family through one complete prior player turn, and independently replay that segment. Returning farther to legal deployment is Ticket 16; the old 159-transition score oracle and assassination oracle remain comparison evidence only.
- [x] Add the first exact focus/control predecessor envelope. From an activation target with a warjack at 2 focus, the inverse restores a zero-focus Control start and strictly replays `end maintenance -> end replenishment/Power Up -> allocate -> end control`; the independent whole-route replayer now consumes the saved fresh action identities instead of applying its default control policy. The focused route certifies exactly, while broader fury/leech/upkeep/soul/corpse/hunger envelopes remain open.
- [x] Separate a single explicit round-3 terminal spatial hypothesis from the turn-1 deployment endpoint. The materializer ignores template positions, requires every live model's exact terminal coordinates, validates table/base geometry plus declared leader-control/action-range relations, and strictly executes the terminal action before reverse search. Missing coordinates, out-of-control/range, overlap, incomplete Steamroller state and materialization budgets reject or defer explicitly.
- [x] Implement the first finite assassination-position domain and per-position batch runner. Board/scenario anchors cross action-range and Leader-control inner/boundary bands plus finite angles; every live model receives a fresh exact coordinate without reading template/opening positions. Each strict-materialized round-3 cell starts its own reverse search, while generation, materialization and search rejection/defer dispositions conserve one public denominator. A focused batch generated 2 roots, searched 1 and retained the other as explicit search-budget debt.
- [x] Bind terminal materialization to the upstream rules-v1 static-placement audit, including rotated/circular blocking terrain, obstacle/obstruction overlap and fail-closed missing blocker geometry; do not maintain a second reverse-project terrain legality implementation.
- [x] Generate the first fixed `100/100` Cryx/Fane later-round position corpus: four explicit round-3 Two Fronts roots assign fresh coordinates to all `103` models without reading deployment coordinates, preserve `17` unit formations and `11` conservative controller relations, and pass parent static placement plus unit LOS/coherency audits. One root is strict-materialized and three remain explicit materialization-budget debt; none is yet a complete reverse-to-deployment witness.
- [x] Reverse the first fixed 100-point root through its terminal attack, Life Drinker lifecycle choice and focus control phase. The concrete frontier retains both use/decline paths, seven strict receipts and a round-3 control-start state; warcaster replenishment, six-warjack Power Up and non-replenishing focus support are exact under `reverse_focus_control_baseline`. The special-rule attack is only a strict existence witness with probability interval `[0,1]`; 24 out-of-scope terminal action variants and every prior turn remain explicit unresolved debt, so Ticket 09 stays open.
- [x] Reverse the explicit `(18,21)` fixed assassination root through one complete prior player turn. Strict root materialization now derives the `0:3` round-3 score from player2 turn-2 settlement and executes player1 Control/Power Up before activation; reverse search reaches player2 turn-2 Control at `0:0` through 15 pass activation groups, publishes the reached frontier, and independently strict-replays the whole segment through Life Drinker. This is not a deployment witness, probability result, alternate-order exhaustion, score-terminal result, or multi-position result.
- [x] Reverse one fixed score root through one complete prior player turn. Four round-3 Two Fronts roots are generated from scenario/control relations without opening coordinates; one root strictly settles `2:0 -> 3:0`, reverses to player1 round-3 Control at `1:0`, closes `19/15` fixed-roster activation groups and seven Reposition declines, and independently strict-replays the full segment. The remaining three score roots are materialization-budget deferred.
- [x] Resolve Wayfinder Ticket 09. Both fixed `100/100`, `103`-model assassination and score terminal families now have one oracle-isolated round-3 spatial root, a concrete reverse segment through a complete prior player turn, and an independent rules-v1 strict replay. Alternate position roots, exact assassination Chance, broader terminal actions, multi-turn deployment reachability and cross-scenario/roster coverage remain explicit Ticket 16/17/18 debt.
- [x] Resolve Wayfinder Ticket 10 with a real-time interactive report and fail-closed training contract. The local console selects assassination/score roots, streams append-only worker events, renders a clickable reverse DAG and 103-model board timeline, exposes every reject/deferred/unresolved denominator, accepts validated `user_injected` terminal seeds, and supports real pause/resume/cancel plus interrupted recovery. A full micro deployment route exports 27 decisions/28 states only when strict replay, assumptions, probability and opponent closure all pass; failed gates export zero candidates and zero negative examples.
- [x] Upgrade the Ticket 10 console into an operator-facing victory-path workbench. It now groups game/victory/root selection, live frontier stages, strict candidate cards, evidence-only filtering and ordering, branch comparison, automatic board playback, readable piece labels, terminal assumption provenance, and quick or exact seed injection. Each published branch carries exact action/score/damage/resource/removal summaries with `strategyScorePresent=false`; resuming at an unchanged budget republishes the checkpoint's independently replayed candidate instead of erasing it from the report.
- [x] Close the first Ticket 16 multi-turn proof for both fixed 103-model terminal families. The assassination and score roots each reverse through two complete prior player turns, preserve their distinct score history, and independently replay the entire segment under rules-v1 strict with zero rejected branches; neither result is yet a deployment witness or an exhaustive action-order proof.
- [x] Make Ticket 16 state/history identity explicit. Missing models, remaining boxes, warjack systems, warbeast branches, RFP, reserve and disabled lifecycle each change the semantic state hash; converged activation orders retain two route labels over one state, with separate probability, resource, score, adversarial-context and provenance evidence.
- [x] Add the first Ticket 16 resume and disposition gates. A micro assassination route stops at a zero-turn budget with four persisted frontiers, resumes without replaying the root prefix, reaches legal deployment and passes full strict replay. The shared report protocol distinguishes strict rejection, budget deferral, low-probability pruning, inverse unresolved, rules unknown, rules drift and proven unreachable, with separate recovery/invalidation behavior.
- [x] Resolve Wayfinder Ticket 16. The console persists and resumes a real 103-model budget frontier (`resume_checkpoint_loaded`, `resumeAccepted=true`), while current-turn and long-horizon semantics are explicit modes rather than inferred from depth; changing semantic mode invalidates the checkpoint, increasing budget within a mode resumes it.
- [x] Generate finite batches of round-3 position cells from leader abilities/control, action range, LOS, base/terrain and scenario-control relation partitions, then run one reverse search per strict-materialized cell with a complete proposed/materialized/rejected/deferred denominator.
- [x] Build the first complete seven-scenario Steamroller terminal skeleton corpus: four terminal classes, `1,618` stable cells, an explicit Two Fronts `8+` tail, source-unresolved boundaries, targeted scenario/rules drift invalidation, and a conserved lazy denominator rather than eager search jobs. Its original denominator was `15,375,441,600`; score-source refinement below supersedes that count.
- [x] Add scenario-specific mutable-state partitions and every currently mapped scoring source, including Kill Box: Trench Cache lifecycle; Wolves progress/token/movement; High Stakes countdown/fuse/blast closure; Payload lifecycle/movement/Made to Haul.
- [x] Strict-materialize and independently replay one score-terminal root for each of the seven scenarios, then reverse every root to the exact enumeration-prepared pre-settlement state. All seven return `1` strict candidate, `0` rejects and `0` unresolved. High Stakes now additionally reverses empty detonation, nonlethal single-target damage, target-only lethal lifecycle and two-target damage with exact joint probability; lifecycle effects outside the damaged target remain fail-closed with `0` candidates and one explicit inverse gap.
- [x] Expose all seven scenario coverage ledgers in the search console while keeping the six coverage-only scenarios non-runnable. The console reports the full lazy denominator and clearly separates a selected future scenario from the currently displayed Two Fronts report.
- [x] Add the first non-Cartesian representative selector. Deterministic skeleton set cover and legal-baseline plus one-dimension-at-a-time selection cover every declared skeleton/partition-value obligation without Cartesian enumeration. The original `2,973/15,375,441,600` count is historical; the current refined denominator is recorded below.
- [x] Publish per-scenario representative counts in the console and verify actual Chromium rendering at desktop `1440x900` and mobile `390x844` with no horizontal overflow.
- [x] Rebuild the seven score-terminal anchors from live rules-v1 execution when the console starts and feed their exact per-cell dispositions back into the coverage ledger. The report derives its seven strict-materialized roots and remaining deferred mass from the current corpus; stale or failed roots cannot be counted by a hand-authored total.
- [x] Turn the console report into a player-readable Chinese strategy view while preserving evidence boundaries. The default view states the result, route size, strict replay status, probability evidence and unresolved risk; `[0,1]` is displayed as `待估`, not as a win-rate estimate. The board exposes every step with previous/next/play controls and lists every discovered branch sharing the current state.
- [x] Replace the report's false presentation fallbacks with exact evidence. Old snapshots recover card/model identity only through same-attempt `pieceKey -> checkpoint.cardSnapshot`; fuzzy name/substring portrait matching is forbidden. The current 103-model report binds `103/103` exact identities and hash-verified official portraits with zero conflicts/gaps: all `16` card UUIDs and all `103` model instances use report-local frozen assets traced to current `40042` official media archives. `verify:search-console-media` checks the current data snapshot, three archive hashes, every archive member and every frozen copy. A path string without a real file is reported as `missing_exact_card_asset`; the browser may not reconstruct an unchecked fallback URL. Because a search root without an evidence-bound table image has no right to borrow one, that branch draws only its exact 48-inch rules-state geometry.
- [x] Supersede synthetic Steamroller presentation geometry with exact official evidence. All seven Steamroller 2026 map diagrams and all printed objective/cache/flag-fallback centers are bound to the current official publication archive with member/archive/image SHA-256 and table-edge-to-base-edge conversion evidence. Two Fronts selects the existing `center_obstruction`, whose center is `0.718609` inches from its official flag and whose geometry overlaps the flag (`0` closest distance), instead of placing a duplicate 30mm obstruction; unresolved selection and invalid fallback branches fail closed. The square-board browser gate verifies the current 49-step strict report, `103/103` identities, `103/103` portraits, zero asset/request gaps, a nonblank `600x600` canvas, Y-axis inversion and stable `?session=` replay.
- [x] Replace the seven score-terminal report tokens with exact Sepsira and Nymara card/model identities projected from cited legal `100/100` fixed rosters. The projection retains identity, stats, health, base and native resource plus full source-piece/card hashes, while explicitly omitting attack, feat, spell and special-action families; it is a strict turn-end scoring micro domain, not a legal two-model roster or full-card tactical root.
- [x] Preserve scenario decision patches across historical turn-end inverse edges and public replay. This fixes Payload prior-turn replay losing its explicit objective-move decline; all seven score roots now reverse to the prior-turn frontier and independently strict-replay with their exact decision witnesses.
- [x] Recheck live data and presentation assets after the report upgrade. The remote updater reports current version `40042` as `no_op`; the report gate verifies the `40042` card snapshot, all `16` exact card images, all three official source archives and every frozen copy by SHA-256. Chromium desktop `1440x1000` and mobile `390x844` render the official Payload map and exact Sepsira/Nymara portraits with zero missing assets or horizontal overflow.
- [x] Fail closed when an archived report's Host receipt differs from the current rules-v1 Host. Historical branches retain their old replay receipt for inspection but publish as `rules_drift`, contribute zero current strict branches and remain non-training until rebuilt and independently replayed under the current Host.
- [x] Bind the seven live score roots into the representative execution denominator. Exact evidence pins, stable task identities, bounded proposal ordering and prior-outcome recovery conserve both selected and global corpus mass. The original pre-refinement `2,980`-task count is historical; current counts are recorded below. The score anchors carry an explicit two-Leader micro-roster receipt, and a different roster receipt is `input_invalid`, not a Host rules rejection.
- [x] Rebase the representative ledger after score-source, action-range, simultaneous-Leader, fixed-round, Scenario Terrain setup and scenario-control relation refinement instead of retaining stale hard-coded totals. The current corpus contains `737,841,700,800` subcells and `18,526` selected representatives: `21` strict-materialized, `14` current-Host strict-rejected, `943` round-equivalence unresolved and `104` source-unresolved. An eight-task proposal budget reports `17,436` budget-deferred plus `8` proposed. Remaining next dispositions are `49` expected Host rejects plus `17,395` ordinary strict candidates. Gates derive conservation from current corpus/tasks rather than fixed totals.
- [ ] Expand Ticket 17 beyond the current evidence batch: select representative source/mutable-state cells lazily, generate score and assassination geometry domains, and cover life/resource/casualty/controller/LOS/terrain/control relations without pretending to execute the audit denominator eagerly. All seven printed layouts are official-coordinate-bound. Scenario Terrain setup now has five exhaustive multi-flag classes plus Fault Line `not_applicable`; Pressure Point has one runnable four-flag/two-candidate/nonfirst-selection root. Remaining setup debt is runnable roots for fallback/selected mixtures and first/single-candidate classes on additional scenarios.
- [x] Separate structural terminal cells from concrete execution-roster evidence. A reusable witness now binds the current Host to two legal `100/100` rosters, `103` card-backed models, exact list identities and legal deployment provenance without claiming later-round reachability. One exact Two Fronts round-2 active-attack assassination representative uses that complete witness, passes Host static placement, executes `melee_attack -> boxed Life Drinker decline -> terminal`, and independently replays to the same state. This proves one subcell only; the two-Leader score projection still cannot supply attacks or certify simultaneous removal.
- [x] Correct the assassination action-range partition after a live Host counterexample. Being outside the direct melee profile does not imply rejection because `advance_then_melee_attack` may still be legal; the partition is now `outside_direct_action_range_requires_prior_movement`. A second exact Two Fronts round-2 active assassination root begins with a 1.25-inch edge gap, executes `advance_then_melee_attack -> boxed Life Drinker decline -> terminal`, and independently replays. Two reactive-assassination representatives with actual base overlap/out-of-bounds states are separately current-Host rejected as `BASE_COLLISION_V1` and `PIECE_BASE_OUT_OF_BOUNDS_V1`.
- [x] Materialize the first simultaneous-Leader-removal representative with the complete execution roster. In official High Stakes round 3, the center 50 mm objective detonates during `end_turn`, explicit blast rolls remove both one-box Leaders, ordinary scoring changes `2:5` to `4:6`, and the Host awards player2 the victory-point tiebreak. Independent replay reaches the same semantic state. This exposed and fixed duplicate turn-end/final-assassination scoring: assassination now reuses the already completed turn-end settlement instead of detonating and scoring twice.
- [x] Close the parent-Host Critical Slow blocker exposed by the attempted Strygon Toxic Spittle witness. Exact current data now applies base DEF 5 for one round only to independently critically hit living models, rejects source drift, and keeps `Damage Type: Corrosion` distinct from `Continuous Effect: Corrosion`.
- [x] Freshly materialize and independently replay the Strygon Toxic Spittle simultaneous-Leader hypothesis with the complete `100/100`, `103`-model roster. The primary miss, independently critical spray-secondary hit, two explicit boost declines, Critical Slow, lethal lifecycle and High Stakes VP tiebreak all execute under the current Host with identical receipts. This exposed and fixed loss of validated `steamroller2026Decision` across internal pre-roll windows; only that validated decision is sealed into the locked source action.
- [x] Materialize and independently replay the simultaneous-Leader Scenario Presence tiebreak with the complete roster. Final VP ties `4:4`; Host computes player1/player2 Scenario Presence `0:6` from exact unit/model contributors and resolves player2. Equality after both VP and Scenario Presence remains source-unresolved.
- [x] Correct fixed-round source classification and materialize one exact VP root for each of the six fixed-length scenarios. Official common VP/Scenario Presence tiebreak text plus each scenario's Defender-round-seven end makes decisive results source-confirmed; equality after both tiebreakers remains unresolved. Each complete `100/100`, `103`-model root passes static placement, Host `scenario_round_limit` terminal execution and independent replay. Two Fronts remains non-fixed.
- [ ] Expand the concrete execution-roster witness across the remaining assassination causal/geometry/lifecycle/resource representatives and simultaneous-Leader causal branches. Every later-round authored state still needs its own Host execution and eventual reverse reachability proof. The completed Strygon and High Stakes roots prove exact action subcells but not deployment reachability.
- [x] Enforce the Ticket 17 Host-authority boundary. The corpus presets complete pieces, status, resources, timing, terrain and exact positions, while interaction/local relation checks only select or filter hypotheses. Only rules-v1 Host static audit or strict action replay can emit `strict_rejected`; local relation misses remain `proposal_filtered` after a successful Host replay, malformed state is `input_invalid`, and omitted work is `budget_deferred`. A focused fixture proves Host-owned Stealth automatic miss, True Sight interaction and terrain/model LOS rejection with zero search-side rule implementations.
- [x] Apply the same authority boundary to score-terminal materialization and execute the first expected-reject representative batch. Roster/history/corpus defects are `input_invalid`; a legal Host transition that misses the requested terminal, score source or partition is `proposal_filtered`. Twelve exact representatives across the six fixed-round scenarios now reach Host static placement and return genuine `strict_rejected`: six out-of-table loser Leaders and six loser Leaders overlapping exact blocking terrain. Their current receipts are included in the production console ledger and recover by stable task identity.
- [x] Add current-Host Scenario Terrain setup authority and one Pressure Point executable branch. The Host now recomputes each flag's candidate set in setup order using exact circular/rotated terrain geometry, removes terrain selected by earlier flags, distinguishes closest-geometry distance from center distance, and fails closed on stale candidates or invalid fallback. A four-flag fixture gives every flag two valid terrain candidates and selects the nonfirst candidate for all four; two historical score windows, terminal settlement, independent replay and exact terminal predecessor all pass. A regression locks the case where center distance is `6` inches but closest geometry distance is `4.819`, which is legal under the official “any portion within” rule.
- [x] Execute the remaining score-terminal `scenarioControl` relation classes under Host authority. Two Pressure Point roots use real fixed-roster card projections and score a separate Scenario Terrain element from `2:0` to `3:0`: one has eligible models from both sides contesting the center 50 mm objective with no securing side, and one has a real Fane solo in range but excluded from control/contest by exact standard-building occupancy. Both independently replay and reverse to one exact terminal predecessor; the Host report, not search-side geometry, supplies contest/securing/exclusion evidence.
- [x] Promote the current ruleset snapshot only after reviewing deliberate executor/card/interaction changes. The reviewed `remote-40041-v5` snapshot, semantic impact, typed graph, strict gates and skill gates now pass; reverse evidence remains non-training truth.
- [x] Replace recursive threshold handling with a layered V3 frontier evaluator. It merges only equal depth/state/cursor/continuation/adversarial-context labels before applying a nonzero threshold, keeps owner alternatives in separate contexts, bypasses thresholding for already-known terminal/unresolved suffixes, and then solves Chance plus AND/min bottom-up. At threshold `0`, the real two-attack fixture exactly matches V2 at `1225/1296`; at `0.03`, both selected adversarial ledgers conserve `success + failure + pruned + unresolved = 1` and retain four pruned labels with incoming contribution evidence.
- [x] Persist V3 canonical states, route labels, edges, every original dice-class Chance contribution and full strict receipts through the external DAG. Recovery restores depth/cursor/context/continuation/exact mass plus canonical state; a resumed V3 run uses the original label identity and does not replay the root prefix. The micro fixture restores 13 labels and immediately executes the purchased second attack. The fixed Excarnate batch persists `V=85/G=85/E=84/L=85/C=119/U=84`, restores all 84 labels/states, and resumes directly with Sepsira's legal advance.
- [x] Remove duplicate successor normalization: the shared strict benchmark adapter now exposes the same normalized state already computed for its receipt, while retaining the legacy raw `.state` field; the frontier step consumes that canonical result.
- [x] Stitch multiple resumed continuation reports back into the ancestor Chance/AND-min graph by semantic label identity. Exact same-context descendant convergence unions incoming edges and rational mass once; an expanded suffix may replace only an exactly matching budget-deferred suffix. The stitch recomputes both four-part mass ledgers, rebuilds the runtime checkpoint, and writes immutable report snapshots with state/receipt references. The restart verifier advances `1 + 2 + 10` restored suffixes through four external-DAG generations and exactly reproduces the full two-attack result `1225/1296` with zero resumable labels.
- [x] Add bounded continuation parallelism with isolated child processes, local-file policy factories, immutable disjoint label inputs, parent-only canonical stitching and a later single-writer checkpoint commit. The micro fixture proves two workers produce the same report/runtime hashes as the sequential batch and propagate a missing-factory failure. The real fixed batch completed 8/8 labels with four workers and zero strict rejects; no child writes the DAG.
- [x] Prove all-or-nothing parallel recovery and make generations structurally incremental. A two-worker fault fixture completes one label and deliberately fails the other, leaves `CURRENT` unchanged, restores the old report, then deterministically recomputes the same sequential hashes. State/receipt and immutable semantic-label/edge/Chance/unresolved objects now reuse verified parent content addresses; mutable mass/status/interval/expansion stays in the report snapshot. Fixed generation 9 added exactly 8 states, 8 receipts, 8 edges, 8 semantic labels, 8 unresolved records and zero Chance objects.
- [x] Add selected-frontier lazy restore for bounded batches. The fixed runner restores all lightweight report/label metadata but materializes only the canonical 8 selected states, keeps old state/receipt bodies behind verified content references, and hashes only new or selected states. The same real 8-label generation fell from roughly five minutes to about 56 seconds with zero strict rejects and unchanged mass accounting.
- [x] Close ticket 11 supervisor/recovery gates. The real generation-11 batch records 8/8 completed tasks, 40 status updates, 16 parent-observed liveness probes and a released lease. A SIGKILL fixture leaves a running record and stale lease; a new process takes over the same run key as attempt 2, retains the interrupted attempt summary and completes. External-DAG fault injection and ruleset dependency-drift gates also pass.
- [ ] Optionally continue the real fixed assassination oracle in bounded persisted batches after the terminal-rooted critical path. Generation 11 at `.scratch/fixed-frontier-ticket09-v4` has strict-executed Sepsira advances on 70 labels with zero rejects; this forward frontier is retained for later candidate comparison and never reported as reverse progress.

## Typed Interaction Graph V2

- [x] Audit the current evidence graph, atom graph, and terminal relevance closure failure mode.
- [x] Re-evaluate the existing graph prototype under the V2 complete-state and Capability Bottleneck contracts.
- [x] Decide typed node/edge vocabulary, graph ownership, and the 126-entry fixed-roster classification denominator through the named V2 graph ticket.
- [x] Generate canonical semantic, evidence, roster, terminal-query, and bottleneck projections from a Steamroller-bound Warmachine Host receipt.
- [x] Prove ordering-set preservation on focused strict traces and keep all graph-based hard pruning disabled pending source, reaction, and broader route closure.

## Custom Matchup Ticket 06 - Assassination Root To Opening

- [x] Add the missing historical terminal gate. Persisted Steamroller scoring ledgers are audited at every turn-end window, and full replay, reissue, prefix recovery and materialized-root entry now reject any victory before the declared terminal edge. The raw `4:0` state still terminates at player2 turn 5 and cannot be resumed unchanged, while its action witnesses may be conditionally reissued only after a separately typed nonterminal history hypothesis changes the exact state.
- [x] Execute the scenario-victory threshold matrix for all seven Steamroller 2026 profiles: opponent-turn lead 2 continues, opponent-turn lead 3 terminates, and own-turn lead 3 continues. The reverse terminal corpus contains assassination, simultaneous-Leader and score victory for all seven profiles plus fixed-round victory for the six profiles that define it; previous-turn inversion and whole-route replay/reissue/prefix recovery fail closed on an undeclared early terminal.
- [x] Reject both the v30 `2:0` row-removal proposal and the v31 counterlead continuation. Although v31 retained the old Cryx score rows, added a typed player2 round-3 center-objective 2-VP obligation and replayed the five-layer suffix, layer 6 strictly settles an additional player1 `pressure-c` VP. The fixed score windows then require the earlier difference to satisfy both `d >= -2` and `d <= -3`; v31/v33 are diagnostics, not resumable history.

- [x] Restore the real 80-model terminal attack, current-turn activation sequence and control boundary under the current Host without consulting prior receipts as execution input.
- [x] Add Host-proposed visibility-graph reverse paths and prove Sepsira's blocked direct run can use the legal three-segment route from `(12,20.25)` to `(12,12)`.
- [x] Add the opt-in Host-enumerated resource-spend prefix family. The real route restores Sepsira to 7 Focus, strictly misses `Excarnate + Breath Stealer + Breath Stealer`, then runs; the resulting state exactly matches the supplied successor.
- [x] Rebuild the source-bound layer-0 checkpoint and close layer 1 with two prior-turn frontiers. Under the v6 bounded search contract, the deepest label has 39 strict edges: terminal 1, control 2, previous-turn end 1, pass 30, unit/model movement 4 and resource-prefix movement 1. Sepsira's spell-spend run and four six-model Swarm runs are Host strict witnesses; every omitted deployment slot, unit anchor, resource prefix and candidate remains unresolved.
- [x] Fix occupied-nearest deployment-slot ordering and paid-run resource inversion. A legal alternate slot is considered before blocked slots consume the origin budget; Warjack run prioritizes predecessor Focus `successor + 1`, Warbeast run prioritizes predecessor Fury `successor - 1`, and Host exact successor equality still decides acceptance. Add finite accepted-candidate and Host-attempt budgets only for explicitly bounded search calls, with every unexecuted combination recorded as unresolved; default callers remain exhaustive.
- [x] Prove real one-layer historical parallelism on the 80-model task. Two isolated workers expanded the two depth-1 frontiers concurrently in about 22 minutes, parent-only all-success merge produced four distinct depth-2 frontiers and checkpoint `e729c432...`, and the deepest route has 58 strict edges including six whole-unit run edges. The focused fixture also proves one-child failure publishes no merged checkpoint.
- [x] Add hash-bound per-frontier task checkpoints and exact-state grouping to historical parallel layers. Each completed child result is atomically persisted without publishing a partial merged search checkpoint; an exact restart reuses completed task identities and runs only the remainder. Route labels sharing one exact state now use one cached expansion while retaining every history and proportionally preserving per-route budgets. Focused fixtures resume from `1/2`, execute only one child, reproduce the uninterrupted merged checkpoint hash, reject tampering, and preserve six output routes from three input labels over two exact states.
- [x] Add activation-label checkpoints inside each historical exact-state task. After one complete activation label expansion, the child sends its queue/boundary/visited/disposition snapshot to the parent, which folds it into the same atomic task-progress file; restart repeats at most the active label and never splits one Host strict operation. Focused interruption and parallel fixtures resume one in-progress activation checkpoint, reproduce uninterrupted report/merged-checkpoint hashes, and reject both inner-checkpoint and completed-result tampering.
- [x] Remove the real activation-checkpoint IPC bottleneck. A child writes each large gzip JSON snapshot to a private content-hash spool and sends only its validated path/hash to the parent; the parent deletes the transfer after atomically publishing the authoritative task checkpoint, and no child writes shared search state. The 80-model layer-3 run emitted 20 activation checkpoints with zero residual spool files and no structured-clone stall.
- [x] Close the first real depth-3 historical frontier under the current Host. The stale 58-edge layer-2 route was not hash-patched: all 58 edges were freshly reissued through 76 strict transitions first. Finite angular control-range event cells found a legal free-run origin for Malefactor outside Power Up range; empty upkeep audit residue is semantically transient, while nonempty residue remains binding. The layer then produced two route labels over one exact state, and both complete 79-edge histories independently replayed `133/133` fresh strict transitions to the same assassination terminal. This is depth-3 route evidence only; no legal deployment, natural probability or strategy-value claim is made.
- [x] Diagnose the authored `0:0`, interim `3:0` and former `4:0` ledgers through current-Host replay. The `4:0` branch proves an earlier player1 scenario victory at player2 turn 5 and therefore disproves, rather than supports, a later Vordak assassination on that route.
- [x] Complete the first current-ledger real layer 4. One exact-state worker expanded the shared depth-3 state in about 17 minutes, emitted `18` activation checkpoints and returned `8` prior-turn frontiers. The deepest route has `98` strict edges, including `21` movement edges and `9` whole-unit runs; `294` unresolved and `57` rejected branches remain explicit. After the complete `4:0` ledger and source update, all `98` edges were reissued through `161` fresh current-Host strict transitions before further expansion.
- [x] Reissue the five-layer suffix under the v31 condition as a diagnostic. One route reissued all `119/119` edges through `211` fresh current-Host strict transitions, and the eight old labels shared one semantic frontier state plus one ordered execution witness. Layer 6 then discovered the mandatory player1 `pressure-c` score and disproved extension of that condition, so v33 and raw `e62497d4...` are not resumable checkpoints.
- [x] Freeze the disproven round-7 Pressure Point route as diagnostics and close Ticket 06 with a shorter 78-model Two Fronts witness. The score remains `0:0`; player2 turn 1 reverses all `17/17` activation groups through Ashmael `shed_fury -> run` and Vordak `run`, player1 turn 1 reverses all `17/17` groups through Sepsira `run`, and the route reaches one strict legal deployment. Independent current-Host replay certifies the complete assassination route, oracle isolation passes, and report hash is `7ed036e32a003d8141d62166ac003691596240c5d335024b252d7602f3b90b00`. The receipt-bound composition preflight now rejects a locally valid activation boundary before witness early-stop when the complete side has illegal deployment geometry, then continues the queued Ashmael/Vordak origin combinations until the strict whole-side boundary passes. Omitted branch mass remains `402` unresolved rather than becoming unreachability.

## Custom Matchup Ticket 19 - History-Bound Terminal Hypotheses

- [x] Separate abstract terminal representatives (Ticket 01), strict terminal-root materialization (Ticket 05), branch reachability (Tickets 06/07), and history-bound score/geometry hypotheses (Ticket 19).
- [x] Add fixed-window score-difference feasibility analysis and use the real layer-6 `pressure-c` settlement to prove that score-only rewriting cannot repair the current five-layer suffix.
- [x] Decouple Ticket 06 existence closure from Ticket 19 completeness. Ticket 19 owns finite complex-history generation and scheduling; a Host-certified score-free assassination witness may close Ticket 06 independently.
- [x] Implement the finite history-bound preset schema and generator across all seven Steamroller scenarios, then require Host strict settlement receipts before a reverse branch can attach to a preset. Current Ticket 06/07 route evidence passes, and both premature and directly rewritten `4:0` histories fail closed.

## Terminal Predecessor Algebra V2

- [x] Define disjoint minimal Steamroller 2026 assassination and scenario-score certificates with exact requested-winner identity.
- [x] Represent victory-event, activation, current-turn, previous-turn, and previous-round predecessor obligations as typed algebra nodes.
- [x] Separate controlled OR choices, adversarial responses, Chance mass, mandatory triggers, reaction windows, strict witnesses, and unresolved inverse operations.
- [x] Preserve complete roster lifecycle/provenance and strategic exchange labels without promoting subjective intent to rules or training truth.
- [x] Persist canonical rules states, algebra suffix labels, and every unresolved obligation through the external DAG interface.
- [x] Verify strict positive/negative terminal routes, 106-model five-mode structure, safe probability intervals, and unified regression parity.
- [x] Enforce a pure reverse-reachability contract: canonical candidate pagination only, budget omissions unresolved, no strategy imports, and peer hold/block/exchange/withdrawal successors.

## Downstream Search Efficiency Policy V2

- [x] Preserve terminal, adversarial, time, resource, exchange, scenario, spatial, redundancy, construction, unresolved, and proposal-cost dimensions without scalarizing them into rules truth.
- [x] Separate exact rules refutations, declared proposal-universe exclusions, budget unresolved, and ordering preferences.
- [x] Define current-turn exact-coverage and long-horizon best-discovered promotion contracts.
- [x] Require same context, strict witnesses, exact values, extension-monotonicity evidence, and probability-ledger permission before hard Pareto suppression.
- [x] Prove that deliberate exchange and immediate material/score regression remain eligible while exact floors and temporal horizons remain enforceable.
- [x] Remove this policy from the reverse-core dependency graph; it is retained only for optional downstream route comparison.

## Forward Completion Experiment V2

- [x] Compare pure reverse atomic replay, unguided strict forward search, and milestone-guided strict completion on the same finite scenarios.
- [x] Prove direct terminal suffixes bypass the connector and one explicit scoring gap is recovered by `run -> end_turn`.
- [x] Measure strict witness rate, branch waste, Host calls, elapsed time, and omitted unresolved action mass.
- [x] Define connector endpoints, stop conditions, CEGAR feedback, and non-optimality claim boundaries.

## Nested Roster And Deployment Search V2

- [x] Bind finite Force Builder roster pools by content hash and canonical roster identity membership.
- [x] Canonically merge duplicate list proposals while preserving alias mass, invalid mass, coverage, and deterministic sampling.
- [x] Nest roster pairs, first-player alternatives, and finite formation pairs into one conserved proposal-mass ledger.
- [x] Materialize a 103-model fixed opening with exact 100-point ledgers, complete provenance, and strict deployment geometry.
- [x] Preserve direct-model strict history receipts and deferred-model conservative reachable envelopes without unsafe pruning.
- [x] Gate capability-based terminal proposal exclusion behind source, alternative, optimistic-bound, strict-preservation, and wildcard closure.

## Current Evidence Baseline

### Ruleset update maintenance

- [x] Add one reviewed composite ruleset baseline and fail-closed snapshot covering strict Host source hashes, card data and mirror, atom/operator registry, typed interaction graph, fixed-roster projection and construction pool.
- [x] Add a per-entity semantic index and update-impact audit for 1,671 cards, 3,045 described rule sources, 377 atoms, 31 hook families, 56 behavior tokens and seven Steamroller profiles.
- [x] Separate new-card atom reuse candidates, unclassified new semantics, card-rule changes, construction-only changes, atom/hook drift, scenario drift and core-executor drift; exact fingerprints are review hints only.
- [x] Generate candidate index/baseline/impact artifacts without overwriting the reviewed baseline, and fail closed old checkpoint, calibration, coverage, witness and skill claims according to the measured blast radius.
- [x] Verify no-drift, exact-reuse, unknown-semantics, atom-drift, construction-drift and core-executor-drift cases.
- [x] Replace the five-entry Host receipt with the recursive local dependency closure: the current receipt binds 48 executor, atom-definition, adapter and scenario modules, so a nested special-rule or atom change cannot be reported as `no_change`.
- [x] Add an evidence-bound review receipt and promotion command that regenerates and promotes the baseline/index pair only after strict and skill gates; the current reviewed `remote-40041-v5` semantic index hash is `98f2ad5dd41cfb6d1f2df706a2bf65adaba8537e2b67b4a6e0184c184d203cd0`.
- [x] Make repeated Field Marshal rule normalization idempotent and distinguish action input, enumeration-prepared state and normalized transition result in strict receipts. The 159-transition fixed Steamroller benchmark and tamper rejection pass; the benchmark remains performance-heavy and is not a scale claim.
- [x] Close Wayfinder Ticket 12 with one inventory-checked completion command. The current report passes `8/8` groups and `79/79` verifiers under Host receipt `80ce324e...`; bounded research readiness is true while continuous-space exhaustion, natural win rate, strategy ranking and global/faction optimality remain false.

- 378 rule atoms, 463 hook operators, 195 primitives, 31 hook keys.
- Reverse maturity: 1 explicit symbolic, 75 generic symbolic, 15 terminal constraints, 99 conservative numeric bounds, 273 typed obligations only, 0 unknown.
- Existing recoverable demo: 9 strict-legal formation proposals, 1 searched, 8 search-budget unresolved, 2/2 jobs completed, 5/5 strict transitions accepted, no terminal witness at depth one.
