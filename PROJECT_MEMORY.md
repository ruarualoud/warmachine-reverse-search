# Project Memory

- 2026-09-05 strategy-workbench scope: resume via `docs/AGENT_TASK_GUIDE.md`, current custom-matchup Wayfinder tickets and the independent Engine standard. README/AGENTS no longer route development to the historical 13-ticket snapshot or superseded map. The current console accepts only preset references/exact terminal roots; a current-battle-state advisor is NOT implemented. Planned read-only guidance fixes the validated current snapshot as the starting boundary without requiring deployment history, while a desired target scene is a distinct objective. Preserve terminal inverse-oracle isolation, missing-state diagnostics, adaptive opponent handling and current-state receipt invalidation. Player comparison uses pre-action information and action-value intervals, not hindsight dice. Faction optimization, battle advice and target reachability share the kernel but need separate input/claim acceptance under existing03/08/09/10/12/13/20/21. No general real-time/full-game optimality promise, new global ticket, runtime gate or online permission follows from the scope document.

- 2026-09-05 opponent-response/ticket ownership clarification: each canonical ticket now owns its OPT development/acceptance checklist; the research report is an index, not a parallel tracker. Preserve 14 historical completions and 9 open tickets; closed-ticket OPT work is explicit out-of-scope maintenance. Ticket21 owns adaptive, non-anticipating responses across both immediate windows and whole turns, true Host terminal absorption, conservative unknown-choice intervals and a shared victory objective across terminal-root labels. An opponent evading one assassination root does not prove escape from all assassination/scoring outcomes, and per-root minima cannot be summed as union victory probability. Ticket20 supplies owner/parameter/ordering domains, 08 supplies existing value algebra, 22 supplies certified reduction, 12 invalidates discovery dependencies. R21-01..12 and staged21a/21b remain pending; no new runtime certification is granted by these plan edits.

- 2026-09-05 search-principles audit: the canonical plan has 23 tickets, 14 historically complete and 9 open; Ticket23 is 8/12, not the stale 5/12. `docs/research/ticket-search-principles-audit-20260905.md` owns OPT-01..23 and the staged plan: after task-scoped Cryx/Dusk/Convergence rule evidence, develop 20a domain/debt interfaces, 21a sound partial intervals and 22 on-demand refinement before all-domain closure. This task-scoped release receipt is NOT implemented and must not bypass current quarantine. Unknown defender actions keep the lower bound conservative; discarded Chance mass remains interval debt and is never renormalized; path success probability is not opening win probability. Preserve Host-only semantics, continuous/quantized/witness distinctions, and discovery dependencies for data invalidation. The 17-state/66-candidate experiments remain synthetic bounded evidence, not full-match speed or completeness proof.

- 2026-08-29 durable Warmachine Slice 23.8 Search boundary: Engine 23.8a-d dynamically enumerate 3128 current `40041` feat/gameplay/spell/weapon sources, safely reuse 25 exact-text IDs, close all `474/474` registered hook proof roles and make all `385/385` registered atoms proof-exact. Shadow Shift continuous placement is now an Engine Host parameterized action: 24 canonical points are hints, while any supplied cent-inch destination receives execution-time legality revalidation. Current dispositions are 409 exact proof-closed, 137 declared proof-pending, 338 contract-mismatch and 2244 unowned. Search has not yet exposed this helper and has no source-ownership or card-rule fork; it must not treat the Engine inventory as complete action/value/pruning truth. Parent 23.8 Host parity, global Strict, skills, training and online remain false.

- 2026-08-29 durable Warmachine parent Slice 23.7 Search boundary: Search binds current Host `ec575f86...16c0` and owns zero final force, deployment or Steamroller legality. Current child reports are 23.7a `3e63e134...bb35`, 23.7b `83db38bf...ea9` and 23.7c `6f16ece5...125`; parent `e4f8200a...8f33` binds Engine `ee8857ce...e767` over 36 semantics, 23 primitives, 109 fixtures, 67/67 mutations and micro 108/108. Host execution proves scoring windows, ledger rejection, scenario setup, Wolves/Payload movement, High Stakes Chance fail-close and one force-entry -> setup -> first-deployment chain. Search may propose rosters, placements, decisions and predecessor hypotheses, but shared Force Builder/Engine alone accepts successor legality. Ticket 23 is 8/12; card-specific interactions, generated interaction closure, publication parity, values, skills, training and online remain false.

- 2026-08-29 durable Warmachine Slice 23.7b Search boundary: Search consumes Engine-owned setup/deployment legality through `warmachineHost` and owns no final deployment validator. The shared path proves ordered starting roll and player/edge choices, first/second initial then Advance Deployment, exact whole-base placement, atomic Unit spacing, post-deployment Prey and ARC resource initialization against Engine's 11 semantics, 4 primitives, 28 fixtures, 19/19 mutations, 12 focused cases and micro 108/108. Search may generate/order continuous placement proposals, but candidate completeness is unresolved and every supplied placement is accepted or rejected only by Engine. Parity report `43a2a15b...8554`; Steamroller, parent 23.7, values, skills, training and online publication remain false.

- 2026-08-29 durable Warmachine Slice 23.7a Search boundary: Search does not parse point-cost model descriptions or decide final force legality. It consumes the exact shared Root/Engine Force Builder for physical-model composition, weapon-attachment replacement and encounter validation, then verifies real Sepsira/Fane/modular/all-army candidates against Engine's 9 semantics, 9 primitives, 30 fixtures and 18/18 mutations under parity report `30d07c70...bf57`. Search candidate scores remain non-authoritative proposal heuristics. Deployment, Steamroller, encounter-specific composition, values, skills, training and online publication remain false.

- 2026-08-29 durable Warmachine Slice 23.6 parent boundary: Engine 23.6d certifies 13 Essence/special-resource semantics, 13 pure primitives, 30 literal fixtures, 16/16 killed mutations, 9 Engine/Layer3 cases and micro 108/108. Parent 23.6 binds current 23.6a-d as 33 semantics, 36 primitives, 113 fixtures, 50/50 mutations and nine cross-resource interaction cases under Engine source receipt `a1cb332b...50b6` / report `f38cb8aa...e07f`. Search imports exact rules plus Focus/Fury/spell/Essence primitive and Layer3 sources; Host `98ba424d...d6af` passes seven representative spell/allocation/force/identity-isolation cases under report `756e25be...7e6d`, with zero Search-owned resource rules. Ticket 23 is 7/12; card-specific replacements stay Slice 23.8 and global Strict, Q_rule, values, skill, training and online remain false.

- 2026-08-29 durable Warmachine Slice 23.6c spell boundary: ordinary spell/upkeep/animus/feat/channeling is recertified by 7 source semantics, 8 shared primitives, 26 independent literal fixtures, 9/9 killed mutations, focused Engine/Layer3/card-text paths and micro 108/108 under Engine receipt `f7f7ab87...a2f4` and authority `54ab5d64...e394`. Search consumes the exact hashed Engine rules/primitives/adapter and proves representative upkeep/animus positive and negative behavior under Host `860839a1...a931` / report `dda66ab1...e182`, with zero Search-owned spell rules; affected Slice 23.5 attack/damage parity is reissued under `96243cdf...d37c`. Repairs include fixed one-point upkeep, current-data animus identity/access, warbeast once-per-activation use, whole-Unit upkeep/animus replacement, numeric reversal on replacement/expiry and structured leader identity precedence. Parent 23.6, Essence/special resources, card-specific cost/target replacements, Magic Ability timing, global Strict, complete Search values, skills, training and online remain false.

- 2026-08-28 durable Warmachine Slice 23.6b Fury boundary: ordinary core Fury is recertified by 8 source-bound semantics, 9 shared primitives, 35 independent literal fixtures, 13/13 killed mutations, seven historical focused paths, defender-owned transfer continuation and micro 108/108 under corrected Engine receipt `47e7422b...ef5e` and authority `54ab5d64...e394`; the then-current Search Host was `9e2e4f89...d3fe` and parity report `95943a04...1b45` with zero Search-owned rules. Historical leech/Spirit Bond/forcing/threshold/frenzy/transfer/reave/heal/aspect executors are reused, not counted as new implementations. Real repairs use current ARC, require exact 2d6, reject ordinary Construct healing, bind post-transfer Chance to the selected recipient and block Wild non-force Fury gain. Provoke's full spell chain, Elemental Mastery and special reavers remain exact-owner debts for 23.6d/23.8; parent 23.6, global Strict, skills, training and online remain false.

- 2026-08-28 durable Warmachine Slice 23.6a Focus boundary: ordinary Maintenance/Focus is source-certified by 5 semantics, 6 shared primitives, 22 literal fixtures and 12/12 killed mutations under receipt `7b6b5d06...c934`; Engine control replay is 42 assertions, Layer3 ARC replay is 200 assertions and micro is 108/108. Explicit ARC must survive Layer3 mapping, Focus Manipulation sets Focus to current ARC, and ordinary warjack Power Up/allocation share the source-fixed cap of three regardless of caller `resourceMax`. The allocation primitive drives both action metadata and state mutation. Fury, spells, Essence, parent 23.6, global Strict, Search values, skills, training and online experiments remain false.

- 2026-08-28 durable Layer3 resource-stat bridge invariant: a Layer3 token/card's explicit ARC is part of the Engine combat-state contract and must survive token normalization, exact mutation patches and state rebuilds. Omitting ARC silently invokes the Engine default and changes Control Replenishment; the certified regression distinguishes a warcaster replenishing Focus `5 -> 7` from a warlock retaining Fury `5 -> 5` across a full opposing turn. Historical focused verifiers are implementation inventory only: they do not certify Slice 23.6 until official-source semantics, independent oracles, interaction obligations and mutations close.

- 2026-08-28 durable Warmachine Slice 23.5 boundary: Engine certifies the bounded core of 29 attack/damage semantics, 32 primitives, 90 literal oracle fixtures, 36 executor-evidence cases and 97 mutation obligations under source receipt `d08f2225...b8fb`. Damage transfer is a defender-owned response before any newly required recipient-system Chance; Search conserves 12 primary Chance classes, 2 transfer responses and 6 post-transfer life-spiral branches with zero Search-owned rule implementation (current Host `9bcbd810...202b`, report `555d460b...f5f5`). Dragoon replay preserves suffered, recorded and discarded-excess damage and strictly executes parameterized legal dismount placement. Current card-specific attack/data gaps remain Slice 23.8, continuous replacement geometry remains Ticket 20, and full Root/App/Layer3/Search publication parity remains Slice 23.10; global Strict, training, skill and online readiness remain false.

- 2026-08-27 durable Warmachine Search geometry boundary: Search consumes reviewed Engine receipt `030870124e891a084eeac21f79141b4f8fa7bdceab8fd4a0a3aaa246e8e1c988`, covering the focused 288/288 denominator under one stable 679-file execution closure. It does not reimplement full-base board containment, base-edge measurement, path/terrain disposition, LOS witness/occlusion, vertical volumes or Falling. Geometry cells, path classes and movement event words may merge only under Host-bound predicates and exact strict witnesses; finite probes never prove an unsupported continuous complement empty. Thirteen Host-authority/geometry/Unit-order consumers pass under the new receipt. Slice 23.4 does not close Search-wide runtime parity, `Q_rule`, remaining Ticket 23 rules, values, skills, training or online experiments.

- 2026-08-26 durable Warmachine Search automatic-outcome boundary: Search never decides whether automatic hit or automatic miss applies. It consumes the strict Engine `automaticHitMissResolution`; automatic miss and automatic hit are deterministic one-outcome Chance nodes, while an explicit attack-roll action carries the dice support. Witch Mark plus Stealth therefore misses unless an independently applicable rule such as True Sight removes Stealth, and Spell Ward remains an Engine rejection visible in replay. Slice 23.1 receipt `eefdb6c5...174d8` is bounded evidence only and cannot promote `Q_rule`, values, skills, training or online experiments.

- 2026-08-26 durable Warmachine Search timing boundary: Search never derives game-round advancement or attack lifecycle ordering. It consumes the Engine Host successor and ordered events. The Host resolves rounds by actual first/second-player roles and commits `attack_resolved` only after applicable damage, disabled, boxed, destroyed and removal stages. Slice 23.2 receipt `5380afee...3426` proves reversed-first-player and lethal Free Strike parity with zero Search-side timing primitives; it does not promote `Q_rule`, values, skills, training or online experiments.

- 2026-08-26 durable Ticket 23 rule-authority boundary: all current Warmachine rule behavior is treated as implementation awaiting recertification, not as globally trusted Strict truth. Official current rules, Steamroller and card sources must first become implementation-independent computable semantics with explicit timing, state read/write, choice ownership, Chance, duration, priority and interaction obligations. A RuleAtom/consumer/verifier reference proves wiring only; strict certification additionally requires an independent oracle, critical mutation detection, generated interaction closure and parity under one content-addressed Engine receipt across Root, App, Layer3 and Search. Historical Strict, skill, checkpoint, route and value artifacts remain quarantined but preserved until the affected semantic slices are recertified. Ticket 23 blocks Ticket 20 completion, real Ticket 22/21 binding, downstream report/skill promotion and online experiments; it does not itself prove Search-space or strategy completeness.
- 2026-08-26 durable Ticket 23 Slice 23.0 baseline: canonical authority receipt `8b374731fe750dc0f548f4e286775c675caa5068eb49285d896167644238e437` binds the current MKIV text, Steamroller 2026, card snapshot, official rule inventory, interaction graph and legacy coverage audit. It reports `16` fail-closed reasons, including a stale graph rulebook path, legacy self-certification and six absent semantic proof artifacts, so the only current disposition is `rules_semantics_unreviewed`. Search receipts/checkpoints/calibration/training, Root/Layer3 skill promotion and route3 launch consume this false verdict; bounded research can execute but cannot emit current Strict, strategy-value, training or online claims. Source/data tampering changes the authority and downstream receipt identities.

- 2026-08-26 durable random-ROF/Unit-attack boundary: random ROF is an Engine-owned pre-target Chance event per physical weapon instance, never a player-selected attack count. Search validates each factor support, expands the exact Cartesian product, conserves rational mass and strict-applies every outcome before target or CRA grouping. Current Kithguard evidence binds live card data: Slugger is `d3`; resolved values `2` and `3` plus three Grunts and the Sergeant produce nine initial ranged slots, and Granted: Dual Shot gives each aimed model exactly one later slot rather than repeating ROF. Grunt/Slugger slots may be spent individually or in CRA; the Sergeant cannot join CRA but receives its own grant. This closes the local declaration and slot-consumption semantics only. The ordered whole-Unit attack partition, all targets, attack/damage Chance, opponent responses and successor equivalence remain open, so Unit combat completeness and `Q_rule` stay false.
- 2026-08-26 durable parameterized Unit-domain, combined-attack and reaction boundary: Engine owns exact selected-trooper advance/run paths plus complete final placements for every other live Unit member, and strictly enumerates/applies or rejects the exact tuple. The reviewed Engine denominator is `269/269` over `634` source files at source receipt `269e2f2b...`. Search can exhaust the selected mover's quantized static path/event-word domain, cartesian it with a complete cent-inch board-point placement superset using overflow-safe decimal BigInt accounting, and aggregate exactly one completed receipt per Host-declared selected mover for one action family; the two-member advance micro conserves `16 = 4 accepted + 12 rejected`. Engine also enumerates every nonempty eligible helper subset for one current CMA/CRA action, and Search's discrete shell sees those rows, but this does not close the whole Unit Combat Action partition across primary, weapon profile, attack multiset, target, charge class and ordered multiple combined attacks. Search now separately projects Host reactions as an `opponent AND` domain: one real Countercharge action has nine declared use/decline/destination-probe options and all nine strict-execute, while the complete response gate remains false because continuous destinations, legal orderings, exact Chance and compound damage transfer are not closed. Run, charge/failed charge, special movement, real Unit sizes, other non-movement parameters, production-planner integration, external hooks and successor equivalence remain open; any missing combined partition or response axis keeps the applicable completeness gates and `Q_rule` false.
- 2026-08-26 durable movement-event automaton boundary: Engine emits exact ordered enter/exit traces for supported convex rough/hazard regions, including initial/final membership and simultaneous crossing groups. Cent-inch positive path segments and movement cost bound trace length; Search replays exact supplied traces and rejects forged/boundary-ambiguous traces. Strict movement consumes geometric entry order, never terrain-array order, and candidate damage requirements inspect every explicit segment. Pure fire/corrosion simultaneous additions may use canonical audit order only under a Host-signed commutative/idempotent semantic-successor proof; this proves no source rules order. Damage, removal, reactions, gas and unsupported simultaneous effects fail closed. Engine now also signs a conservative start-to-boundary lower bound. When every boundary is farther away than the complete movement allowance, that distance theorem plus one strict zero-crossing witness makes the realizable event-word language exactly `{epsilon}`; reachable boundaries remain unresolved. The current Engine receipt is `6accb763...` (`268/268`, stable `633` files), Search production closure is `75` files, and ruleset impact is only `core_executor_changed` at `e8cda4ee...`. Reaction/external-hook closure, reachable-boundary event languages and transition stability remain false, so Ticket 20 and `Q_rule` stay open and the ruleset baseline is not promoted.
- 2026-08-25 durable fixed-endpoint obstacle path-class boundary: in a convex board/allowance working domain with exactly one strictly interior axis-aligned rectangular obstacle, antipodal actor/fixed-endpoint geometry makes every relative homotopy class an odd-π angular lift around the obstacle center. Every path stays at radius at least the rectangle inradius, so its length is at least `inradius × |angular lift|`; when Host allowance is below `3π × inradius`, only the clockwise and counterclockwise half-turn classes can exist. Search requires one exact quantized Engine strict witness for each and rejects direct crossing. This closes `2/2` classes only for that fixed-endpoint theorem family; arbitrary endpoints, larger allowances/multiple winding classes, multiple or nonrectangular obstacles, events and transition stability remain unresolved.
- 2026-08-25 durable universal open-movement-domain boundary: Engine may certify universal strict execution only for the exact fail-closed domain of one live non-Unit model, empty terrain/scenario/rule/status/event surfaces, canonical board-inset plus movement-allowance predicates, and cent-inch-quantized advance/run endpoints. Every admitted endpoint uses the straight path and is backed by zero-distance and positive-distance strict witnesses; Search verifies the theorem, predicate/actor/action bindings and source receipt. Rough terrain, hazards/reactions, obstacles/models, Units, special rules, scenario state and activation residue cannot inherit the theorem. The new focused receipt passes `268/268` verifiers under a stable `633`-file closure. This proves action execution over that narrow domain, not transition stability, general continuous geometry or Ticket 20 completion.
- 2026-08-25 durable movement-event-surface boundary: strict movement execution effects and Search path partitions must share one Engine-owned source-bound event surface. Fire/acid hazards use the exact circle or rounded rotated-rectangle Minkowski region inflated by actor base plus trigger range and force `movement_hazard_path_partition_required`. Same-endpoint acid crossing/avoidance separates because only crossing applies corrosion; opposite fire/acid routes retain matching Host/strict effect order. Exact basic-advance differences confined to rough-cost/move/window audit events may be elided only after binding successor rule state; every other event list falls back to exact comparison. This does not close the complete event language or reactions.

- 2026-08-25 durable convex-obstacle-nerve boundary: an overlap graph cycle is not by itself a hole. For strictly interior convex exclusion clusters with every pair and every relevant nerve triangle classified, Search computes exact first homology over `GF(2)` and uses Alexander duality to derive endpoint-formula complement component count. A triple-filled cycle has one component; a three-circle ring with pair overlaps and a proven-empty triple intersection has exact `H1=1` and two components. Component counting alone does not materialize or strict-certify components; only the separately proved narrow ring materializer may do so. Touching, unsupported intersections and boundary-spanning exclusions remain unresolved; strict reachability, path topology and transition stability are separate.
- 2026-08-25 durable narrow ring-component materialization boundary: for exactly three circular configuration obstacles with all pair overlaps, proven-empty triple intersection and exact nerve `H1=1`, the center triangle lies in the forbidden union and is a Jordan separator. Search may bind the outer component to an accepted strict path and the center component to a formula-valid point proven unreachable from the outer source. This closes component materialization only for that narrow ring; mixed shapes, multiple holes, touching/boundary cases, weighted path reachability and general strict reachable-set topology remain unresolved.

- 2026-08-25 durable parameterized-movement API boundary: Search must not manufacture Engine-internal `explicitMovementPaths`. Engine `materializeRulesV1ParameterizedMovementAction` owns cent-inch path normalization, exact path evaluation, internal materialization, focused strict enumeration and a hash-bound receipt for one single-model advance/run. Search validates and executes that returned action. One accepted parameter proves only that action; it does not establish neighborhood/universal legality, a complete path class, Unit geometry or transition stability.

- 2026-08-25 durable path-topology-versus-execution boundary: when the exact Host movement domain is only the convex board inset intersected with the actor-centered movement disk, with no configuration obstacles, path events or wildcard debts, every legal path to one fixed endpoint is relative-endpoint homotopic to the straight segment. This closes one geometric path-topology class per endpoint only. It does not prove strict enumeration/application for every continuous endpoint, does not erase exact audit-record differences, and does not prove transition stability. Rough terrain, obstacles and Unit geometry require separate partitions and cannot inherit the theorem.

- 2026-08-25 durable endpoint-connectivity certificate boundary: one strict accepted representative proves only nonemptiness. A nonempty finite intersection of supported convex inclusions has one endpoint-constraint component. For strictly interior convex exclusions, every pair must be proven strictly separated or strictly overlapping; when each overlap-graph component is a tree, the convex good-cover nerve proves that cluster contractible and the planar complement remains connected. Isolated and tree-overlap clusters fit; inflated touching, unsupported pair relations, cyclic overlap and boundary-spanning exclusions remain unresolved. Page-local success is never whole-denominator completion unless a single receipt starts at cursor zero and exhausts all signatures. None of these certificates proves strict reachable endpoint connectivity, path homotopy, ordered triggers or transition stability.

- 2026-08-25 durable exact-path-before-homotopy boundary: every discovered movement witness binds an Engine-owned exact path evaluation containing its endpoint signature, exact quantized path hash, segmented movement cost, ordered terrain-event trace and strict precheck evidence. Search may retain multiple strict paths for one endpoint and must not merge paths whose event signatures differ. Exact-coordinate inequality is a conservative anti-merge guard, not a finite path quotient. Path homotopy, ordered reaction/trigger automata and transition stability remain unresolved until a separate complete partition/refinement proof closes them; discovered routes and failed path probes carry no Chance mass.

- 2026-08-25 durable strict-geometry-witness boundary: Engine, not Search, evaluates each quantized endpoint against the source-bound movement predicate plan and emits a hash-bound predicate/event signature. Search may generate bounded critical-point probes, but a represented signature requires a valid Engine evaluation, a Host path proposal, exact strict action enumeration and an accepted transition landing at the same endpoint. This proves existence for one endpoint/path only. A finite probe miss remains `unresolved`, never proves an empty or unreachable cell, never removes denominator mass and never receives Chance probability. Even when every declared Boolean signature has a witness, connected components, path homotopy/ordered triggers and transition stability remain independently false until proved.

- 2026-08-25 durable Host-predicate/strategy-context boundary: Search never reconstructs movement geometry from coordinates or rule text. Engine `buildRulesV1MovementGeometryPredicatePlan` owns board/base inset, rule-atom allowance, terrain/model primitives and wildcard path debts; Search verifies the canonical Host plan hash, binds the exact Host/state receipt and preserves every debt before any cell partition. The first lazy cell cursor conserves every Boolean signature of declared endpoint event regions while requiring all legal endpoint predicates, but does not claim geometric nonemptiness, connectedness or transition stability. Rule-behavior equivalence and strategy substitutability are separate: the latter additionally requires exact initial-distribution, history, observation, reachability-context and remaining-horizon receipts combined into `strategyContextKey`; missing/different keys cannot merge or pass the strategy-completeness gate. A future-behavior-equivalent but unreachable member cannot represent a reachable strategy. `finiteGeometryCellPartitionReady=false`, `pathClassPartitionComplete=false`, `transitionStable=false` and `ruleBehaviorQuotientComplete=false` remain mandatory until later Ticket 20 slices.

- 2026-08-25 durable strategy-quotient completeness invariant: a finite Host-observable rules quotient `Q_rule` preserves legality but does not by itself justify tactical pruning. A task-bound `Q_goal` may claim preservation of all potentially effective strategy result classes only for a fixed Host/data/map/roster/initiative/horizon/information/strategy/objective receipt. Reverse search must form an over-approximation, not a discovered-route under-approximation. Every `Q_rule` cell is retained or carries a verified unreachable, one-way probabilistic alternating-dominance, or componentwise admissible-upper-versus-strict-guarantee-lower certificate; every other cell is `unresolved` and enters an independent strict forward complement challenge. State merging requires one symmetric probability-alternating bisimulation relation; two independent one-way simulations are insufficient. Exact Pareto output can be super-polynomial or continuous, so a complete quotient is not guaranteed small. `unresolved=0`, closed terminal/objective/player/opponent/Chance domains and zero CEGAR debt are mandatory before `effectiveStrategyQuotientComplete=true`.

- 2026-08-25 durable search-run identity boundary: a resumable search checkpoint may intentionally continue under a larger exploration budget, but reports from different run options are not deterministic-equivalence peers. `warmachine_search_run_contract_v1` hashes Host/search source identity, fixture, terminal cell and every result-affecting option while excluding callbacks and runtime checkpoint stores. Ticket 07 report v3 compares semantic hashes only when this contract also matches. Under Host `c812a00b...`, the expanded Hysene score run now reaches one legal `113`-model opening through `96` reverse edges, independently strict-replays the route, preserves `111` rejects and `1011` unresolved branches, and repeats with identical semantic hash `cc242803...`; report hash after the confirming repeat is `5276b2b8...`.
- 2026-08-25 durable current score-route existence closure: the regenerated Nymara score route under Host `c812a00b...` reaches one legal deployment through `96` reverse edges, independently strict-replays and passes oracle isolation with `15` rejects and `999` unresolved branches; report hash is `fd51e093...` and opening state hash is `3634c156...`. Together with the current expanded Hysene route, all currently targeted Fane score existence cells have a current-Host witness, but candidate, opponent-response, Chance and continuous-geometry denominators remain open, so no initial-state value or optimality claim follows.
- 2026-08-25 durable continuous-domain/search-direction boundary: player-selected coordinates, paths and formations are quantified choices, not random variables, so they never receive area-, length- or sample-frequency Chance mass. A finite geometry quotient is exact only when every Host-observable predicate is constant within each cell, path-topology/trigger classes are closed, and partition refinement proves transition stability: states in one cell expose the same legal action families and the same player/opponent/Chance successor-cell signatures. Representatives are strict execution witnesses only. Any unproved remainder stays as recoverable continuous-domain debt. Multi-turn search therefore combines horizon-indexed reverse target domains with exact-opening strict forward reachable states, selects the expansion direction from same-denominator receipts, and hard-prunes a nonintersection only when the necessary-condition proof is complete.

- 2026-08-25 durable archived Unit-movement reissue boundary: an archived whole-Unit path that current strict execution rejects may be migrated only inside the same Unit activation. Reissue first retries the exact path, then may ask the current Host for a legal selected-model detour; if the original selected model has no legal path, it may select another eligible member as the Unit movement anchor only when action type, resources, Unit identity and every model endpoint remain fixed. The migrated edge must exactly reproduce the source successor semantic hash, the rebuilt route must independently replay, and a forged source successor rejects. This closes real Necrosurgeon Initiates sibling-collision edges without treating the old illegal straight lane as rules truth.
- 2026-08-25 durable Ticket 09 current route-unit reclosure: under Host `c812a00b...`, Auricant/Hysene/Nymara assassination route units freshly execute `53/55/54` transitions with hashes `33cd8760...`, `ba07c36f...`, and `9d381490...`. Ashmael/Auricant score route units each reissue `96` reverse edges through `112` transitions, independently replay, and recertify the settlement history; hashes are `dc15a851...` and `21b329e2...`. These are cooperative reachability witnesses with incomplete candidate, opponent-response and Chance closure, so their values remain `[0,1]`; Nymara score is still missing.
- 2026-08-25 durable source-fixture provenance invariant: an immutable forward fixture may retain an older Host receipt only as proposal/history provenance when a current-Host route report binds its exact fixture hash and independently strict-replays the entire route. Every downstream settlement window must still execute independently under the current Host, and the source receipt must remain explicit in compact evidence. This exception does not promote old route-unit receipts: an archived route unit without a current full-route replay remains stale and is rejected.
- 2026-08-25 durable Ticket 07 current reclosure: under Host receipt `3bf551f7...`, the Hysene score terminal reverses through four complete prior player turns to one strict legal `113`-model opening in `96` edges and independently replays the full route. The selected route report hash is `b8f58c77...`, opening state hash `5c32dbc8...`, with `8` rejected and `1042` unresolved branches. Ticket 19 freshly certifies five settlement windows and seven scoring sources through `0:0 -> 0:0 -> 0:0 -> 3:0 -> 6:1` (report `93bce515...`), and Ticket 08 keeps both real cells at `[0,1]` because candidate, opponent-response and Chance closure remain incomplete.
- 2026-08-25 durable archived-route audit boundary: current static invariant audit of Ticket 09 artifacts finds `167/169` unique states legal. The two illegal states exist only in the unfinished Nymara score frontier and place `player1_mechanithrall_brutes_24_2` overlapping blocking terrain `south_wall`; they must be regenerated, not resumed. Five other route units have no such state-invariant failure but remain stale under Host receipt `539dabd1...` until current-Host full-route replay reissues them.

- 2026-08-24 durable charge-combat inverse boundary: Search now has opt-in `charge_combat_activation_inverse_v1` and `failed_charge_activation_inverse_v1` peer branches. A successful candidate is accepted only after the current Host executes one target-bound exact straight charge lane, its mandatory first charge attack, every selected continuation and activation completion to the exact supplied successor; a failed candidate executes its legal movement and immediate activation end without inventing an attack. Independent, Unit and failed-charge focused routes pass, while an over-distance origin cannot become a strict candidate. Automatic target-ray origins are finite proposals, not a continuous-space completeness claim. Target lifecycle/health preimages, omitted origins, unselected attacks, reactions and Chance classes remain unresolved. The historical Ticket 06 forward witness still hard-codes `run`/`completionOnly`; current-Host audit `7964689d...` proves the concrete run and zero immediate material target actions in two recorded player1 states, not the absence of buff/place-enabled alternate activation sequences, whole-turn response closure or optimality. Its reverse options now admit advance/run/charge-combat/failed-charge peers, pending full real-route revalidation.

- 2026-08-24 durable Ticket 06 action-scope correction: the persisted turn-one Vordak run is rules-legal and reaches farther than any charge (`SPD 7`, run `12\"`, charge `10\"`, nearest enemy center about `32.8\"`). Fresh current-Host enumeration finds no immediate single-activation player1 material action targeting Vordak in the recorded pre-Sepsira-run or post-Sepsira-run states. It does not exclude an alternate activation sequence that first applies a buff, debuff, place or other enabling effect. The forward route hard-codes `run` and sends every unspecified activation through `completionOnly`; its reverse scope now includes advance/run/charge-combat/failed-charge peers, pending real-route revalidation. It remains an existence witness with omitted opponent responses, not evidence that run is generally optimal or that adversarial responses are closed. Ticket 08 correctly keeps the value interval at `[0,1]`.

- 2026-08-24 durable Ticket 09 route-identity invariant: a route cannot be strict report evidence from replay/history booleans or model count alone. It must match one Ticket 08 `routeKey`/terminal label, the persisted current-Host opening state/evidence hashes and full opening address, plus one exact sealed Force Builder roster pair whose two 100-point/model ledgers agree with that address and whose challenger Leader agrees with the route. The real Ashmael assassination and Hysene score routes pass this gate under roster pool `4296fa8d...`; both legal route pairs are outside the current 16 representative pairs and both Fane lists differ from the displayed screening representatives, so they prove leader-family route existence only. Ticket 08 now persists the opening proof instead of dropping it from its compact report. Current report hash is `e3b0480133cd3f76fd834198bdbd6b90d1cb9935aedf4e620ce52271c6ce52d0`; strict routes remain `2`, map debt `0`, route debt `6`.
- 2026-08-24 durable Ticket 09 map-evidence invariant: a declared map profile becomes strict opening evidence only when one exact terrain realization has a content-bound topology audit and then passes current-Host deployment, static placement and Steamroller scenario-terrain binding. The audit uses north-south LOS-blocker projection, a 50mm-reference horizontal movement bottleneck and nonoverlapping rough-terrain area; it is a finite map cell, not a continuous path proof or natural table distribution. Current open/mixed/dense realizations measure lane openness `0.958333333 / 0.791666667 / 0.125`, and the fixed Sepsira/Fane report materializes `288/288` strict openings with zero reject: `72` per Fane leader and `24` per leader per map across three deployments, both initiative sides and four Cryx representatives. Report v2 hash is `bdf3ad1304286ed9ff55da678a57187af124ea20d566f38992c628be8ecc0eb5`; map debt is zero, while six leader-by-terminal-family routes remain missing and no natural win rate or optimality follows.
- 2026-08-24 durable Ticket 08 closure invariant: an initial-state value is exact only when strict opening, complete subject candidate set, conserved and closed Chance mass, complete opponent response sets, assumption/history closure and current-Host strict replay all agree. Chance ledgers retain exact rational success/failure/pruned/unresolved mass; opponent alternatives use `AND/min`, subject candidates use `OR/max`, and any missing closure remains in the interval. Equal strict states may share one rules computation, but roster/map/deployment/route/cost/terminal-source labels remain separate. A user-declared distribution produces only a declared-distribution value interval; this layer never promotes it to natural win rate without a separate verifier. Current Ticket 06/07 cells both remain `[0,1]`: Ticket 06 additionally lacks a persisted compact full-route replay receipt, while Ticket 07 has its `96`-edge replay/history proof but still lacks candidate, opponent and Chance closure. Real report hash is `4720e9778262fd1689e5e02384fae596b11bade185e31d19c61bc5e0227a75c7` and value-set hash is `12e5041ce30d3202f9a8e9ebbca9f13b978c3bc0b6f4a87bc2ff06ad2d90270d`.
- 2026-08-24 durable Ticket 19 closure invariant: a historical score is never an independently editable state field. `warmachine_steamroller_strict_history_binding_v1` derives each declared settlement window from current-Host strict `end_turn`, binds the complete retained score-history prefix, newly added scoring rows, source events and geometry-bearing successor, and independently replays every window before branch attachment. Ticket 06 certifies two `0:0` windows; Ticket 07 certifies five windows and seven sources along `0:0 -> 0:0 -> 0:0 -> 3:0 -> 6:1`. An assassination history that reaches `4:0` early is excluded as `history_bound_terminal_earlier_scenario_victory`, and directly rewriting a real successor to `4:0` is rejected as `history_bound_terminal_successor_state_mismatch`. The combined report hash is `35c030a7857a4a0f039236b238268d4b4841574acf26b40726f101e734f9f9ee`; this proves only supplied settlement histories, not omitted action reachability, natural probability or optimality.
- 2026-08-24 durable Ticket 06 closure invariant: the 78-model Two Fronts short witness keeps score `0:0`, reverses both first-turn activation sequences (`17/17` groups per side) through strict movement/resource predecessors, reaches one legal deployment and independently replays to the same Vordak assassination terminal under Host receipt `539dabd1...`; full-route report hash is `7ed036e3...` and oracle isolation passes. A mandatory composition preflight now checks movement candidates, control resource preimages, turn-boundary activation restoration and the complete player2 first turn before the full search. At player1's first control start, reversing player2's first turn must use `preserve_reset_state` so player1 remains entirely unactivated; later round boundaries may use `all_alive_activated`. A first-turn activation boundary does not count toward a witness-stop budget until the whole side passes strict deployment-zone, base-overlap, unit-coherency, attachment-distance and cross-side-overlap geometry; this lets the search reject a locally legal but globally invalid Ashmael/Vordak origin pair and continue to the already queued legal pair. The witness proves route existence only: `402` omitted branches remain unresolved and no natural probability, value or optimality follows.
- 2026-08-23 durable Ticket 06 ownership boundary: assassination-root-to-opening proves existence for one task-specific root already certified by Ticket 05 and the current strict Host. It must audit and reject an early terminal along the selected route, but it does not own Steamroller rule completion, exhaustive score-history generation or preset scheduling. Those remain with the upstream Host/Ticket 04, Ticket 05 and Ticket 19 respectively; Ticket 06 may close on a simple score-free strict witness without waiting for Ticket 19.
- 2026-08-23 durable historical-terminal gate correction: every persisted Steamroller turn-end ledger must be audited for victory at each historical scoring window, and every strict full-route replay/reissue/prefix recovery must reject a terminal event before the declared final edge. The former `4:0` Pressure Point route reaches a player1 scenario victory at player2's turn-5 end (`lead=4`, required lead `3`), so its later turn-7 Vordak assassination and all five derived historical-layer checkpoints are invalid as one legal game history. Fresh replay of the affected 19-edge suffix reproduces the early terminal and fails closed; edge-local strict receipts do not rescue an already-ended game.
- 2026-08-23 durable score/geometry binding boundary: a historical score hypothesis is not independently editable truth. Every score row must be reproduced by exact model/Leader geometry at its settlement window before a reverse branch may attach to it. Bundle v31 and checkpoint v33 reissued a five-layer suffix through `119/119` edges and `211` current-Host transitions, but layer 6 strictly adds a player1 `pressure-c` VP at player2 turn 4. With the later fixed `pressure-a` and Kill Box gains, nonterminal history requires the earlier score difference to satisfy both `d >= -2` and `d <= -3`; therefore v31/v33 are diagnostic evidence, not resumable history. Repair must change a real control/Leader-position source on a locally re-expanded branch, not rewrite score fields.
- 2026-08-23 durable target-guided resource-prefix origin invariant: when a Focus warcaster's successor has spent resource but its observed destination is outside every selected spell's range, activation inversion must not invent a resource-prefix pass or cast from the destination. It may generate a finite target/range event-cell origin, then ask the Host to execute the exact spell targets and rolls before a legal movement path to the observed destination. The focused Sepsira witness starts at `(12.4282,22.2913)`, strictly spends `7 Focus` on Vordak with `Excarnate + Breath Stealer + Breath Stealer`, and then moves to `(11.44,26.17)`; omitted target/range mass remains unresolved.
- 2026-08-23 durable real layer-4 evidence boundary: after applying the then-known scenario ledger to the historical root, both depth-3 labels were freshly reissued and combined over one exact state. The bounded layer-4 expansion completed one exact-state task, emitted 18 activation checkpoints, reached eight prior-turn frontiers and extended the deepest route to 98 strict reverse edges (`21` movement, `9` unit runs). It retains `294` unresolved and `57` rejected branches, reaches no legal deployment yet and proves neither natural probability nor strategy value. The later complete `4:0` ledger supersedes the interim `3:0` score claim, while the route shape remains historical evidence and was reissued under the corrected source before layer 5.
- 2026-08-23 durable current-Host route-reissue invariant: a source-drifted reverse route cannot be revived by merely recalculating checkpoint hashes. Reissue may use its stored action, roll and explicit-path witnesses, but must freshly enumerate and execute every edge through the current Host, rebuild every predecessor/successor and receipt hash, and exactly match the current terminal state; one failed edge rejects the whole reissue. Empty `upkeepsPaidThisControlPhase` arrays are transient audit residue and are omitted from semantic identity, while nonempty upkeep evidence remains semantic. A Warjack Power Up avoidance predecessor samples a finite angular event-cell decomposition around control range and keeps unsampled radial depth explicitly unresolved.
- 2026-08-23 durable real layer-3 evidence boundary: the current 80-model Sepsira/Fane frontier first reissued all 58 prior edges through 76 fresh strict transitions, then completed 19 activation labels plus one activation-boundary checkpoint without IPC residue. Malefactor's free run used a legal angular-cell origin outside Power Up range, so Control replay preserved Sepsira/Malefactor/Raptor predecessor resources at `0/0/0` and produced two distinct control-residue histories over one exact depth-3 state. Both 79-edge histories independently replayed all 133 current-Host strict transitions to the same assassination terminal. This proves the tested route through three reversed prior turns, not legal-deployment reachability, natural probability, strategy value or global completeness.
- 2026-08-23 durable historical-layer recovery invariant: a terminal-rooted parallel layer may persist a provisional, hash-bound result after each independently completed exact-state task and after each fully processed activation-search label inside a running task. Resume binds the source layer checkpoint, exact target depth, complete task set, task identity, activation root state, side, search options, queue/visited/boundary state and every result/checkpoint hash; mismatch fails closed. Exact-state-equivalent route labels share one worker expansion/cache but remain distinct histories, with route/completion budgets scaled by group size. Reused tasks skip child execution; an interrupted in-progress task repeats at most its currently executing activation label, while one Host enumeration or strict transition remains atomic and is never split. The merged search checkpoint remains parent-only and all-or-nothing. Focused fixtures preserve six output routes from three labels over two states, resume both a `1/2` completed-task interruption and an in-task activation interruption, and reproduce the uninterrupted report/checkpoint hashes.
- 2026-08-22 durable Ticket 05 finite-batch invariant: the current receipt-bound production plan `764d6d64...` schedules 64 tasks from 2,049 demand groups, 18,517 representatives and 13 construction macro profiles, with 39/39 unique strict openings across all seven scenarios. It completes 51/64 as 13 task-specific strict roots, 4 Host rejects, 28 exact relation filters and 6 official-source unresolved results; the 13 unsupported assassination partitions remain `budget_deferred`, preserve the value upper bound and are not unreachability or training labels. The four family counts are assassination 11/24, score 11/11, fixed-round 13/13 and simultaneous-Leader 16/16.
- 2026-08-22 durable moved-combat sequence invariant: a charge or move-then-attack starts one whole-combat initial-attack sequence, exposes every initial weapon identity before the purchased-attack window and binds LOS evidence to the exact selected action variant. The current Vordak witness executes two initial Talon attacks followed by a purchased hit-and-damage-boosted attack for 8/8/14 damage; that third action produces exactly 3 Fury for purchase, hit boost and damage boost, then the boxed Feast decision commits the fatal lifecycle. Primary and independent replay reach the same terminal state.
- 2026-08-22 durable terminal-geometry reduction invariant: geometry candidates may be pre-excluded only by input validation or a Host/static-proven necessary condition, with the exact input and evidence hashes retained. Hard equivalence requires the complete current Host-observed geometry relation; matching base size, symmetry, topology or approximate distance alone is insufficient. Existence search may stop at the first strict witness only when every remainder is recorded as unenumerated, the value remains `[0,1]`, and probability/optimality closure is disabled. Full probability work uses a separate completion contract. Three current full-versus-folded fixtures preserve strict dispositions and semantic replay at `4→2`, `4→2` and `2→1` representatives; these measured reductions are fixture evidence, not a global performance claim.
- 2026-08-22 durable terminal-task behavior-signature invariant: every terminal task adapter must reconstruct the same behavior signature as the batch planner, including exact representative coordinates, execution-envelope hash, Host receipt, construction Host receipt and composite Search execution-semantic receipt. Omitting the final field can misclassify current legal tasks as receipt-drift `input_invalid`. A receipt change requires new strict seeds, plan, openings and checkpoint; results from a mismatched checkpoint are not repaired in place or inherited.

- 2026-08-22 durable Engine/Search receipt invariant: Search is bound to reviewed Engine source receipt `239d80fd55217b752e09c0970df5072400b91f0f3c4a14338f277f2911255962`, whose eight-shard aggregate executed `263/263` declared verifiers (`235` rule-atom plus `28` strict-transition) under one stable `628`-file execution-source closure. The current Search baseline is `warmachine-ruleset-2026-08-22-remote-40041-ticket05-combat-sequence-v1`; ruleset snapshot, candidate completeness and fixed 103-model benchmark pass. This proves that declared denominator only; it does not prove every card interaction, continuous geometry, terminal route or strategy branch.
- 2026-08-22 durable Ticket 16 candidate-completeness invariant: candidate slots are consecutive and mass-conserving across direct and moved attacks, every weapon/spell/rule-granted profile, Stealth/True Sight, terrain/model LOS, Revive forfeitures and parameterized Bulldoze. Production v5 maps all `64` selected terminal tasks to `39/39` strict unique openings across all seven Steamroller scenario maps with zero failed or unresolved openings. Terminal adapters completed `43/64`, including three task-specific strict roots; `21` remain explicit budget-deferred mass, so deployment-to-terminal reachability, exact game value, natural win rate and global optimality remain unproven.
- 2026-08-22 durable Ticket 17 recovery invariant: a failed strict transition is terminal failure even when Host emits a reject receipt; only `transitionOk=true` receipts enter the committed prefix, while rejection evidence remains separate and the candidate cursor does not advance. Assassination primary/replay progress and batch task progress are sealed against task behavior and execution receipt. Production checkpoint publication is single-writer, CAS-bound, fsynced and atomic; stale locks are recoverable and unpublished files are ignored. Under Search source receipt `e4c6ee84a502407ba5e352cd71921c80446a7e2f930150367bddcdf01f74d504`, a real 78-model route recovered through nine checkpoints and exactly matched one-shot report `d5a12478323594f4f1f0ec2af32a449392cc0ca7fe2f1151a5ed72c9198abed9`. This proves recovery semantics for the tested route and injected failure points, not every terminal family or OS failure.
- 2026-08-21 durable Ticket 15 Search-only promotion invariant: the bound reverse-search materializer/planner/executor/recovery closure is algorithmic and has no external model, prompt, HTTP Provider or skill-router ingress. Its ruleset promotion therefore treats ctx2skill/skill2ctx as not applicable to Search, without certifying route3, self-play or human-vs-AI skills. Fixed benchmark inputs default to Engine receipt-bound `fixtures/ruleset-baseline` artifacts rather than historical `build` output; the six-verifier cross-repository clean baseline must fail closed on Engine/source/checkpoint/snapshot drift or missing inputs.

- 2026-08-15 durable multi-source-score-root invariant: a task-specific Steamroller score root must prove the exact requested scoring-source multiset, not merely the same net score. The current Payload root explicitly declines both payload moves and leaves exactly one Raptor with one Focus; the Trench Warfare root reserves only the five Ambush-eligible Merciless models, keeps the non-Ambush attachment in play and both caches active. Geometry parking may not move the ending-side Leader out of the killbox, because an undeclared killbox penalty changes the terminal source ledger.
- 2026-08-15 durable terminal-action-evidence-binding invariant: every terminal partition relation must be evidenced by the actual terminal action. A clear ranged sibling action cannot certify `lineOfSight=clear` for a lethal melee action. The current assassination adapter therefore remains outside production until action-bound LOS evidence and its focused execution cost are closed.
- 2026-08-15 durable simultaneous-candidate-filter invariant: maximum-roll strict execution of the current complete-roster High Stakes Strygon candidate leaves the two Leaders at `9/12` health and does not produce a terminal event. This is an exact `proposal_filtered` disposition for that task candidate only, not a Host reject or evidence that the simultaneous-removal family is unreachable.
- 2026-08-15 durable initiative/round-role invariant: Steamroller first player and Attacker are one coupled relation; side permutation maps canonical roles onto task rosters and is not an independent second initiative axis. A turn transition completes a round only when the side that did not go first ends and play returns to the first player. Both terminal materialization and previous-turn inversion derive the predecessor round from this relation rather than hard-coded player numbers. The corrected `64`-task plan has candidate mass `10,628,352`, exactly half of the invalid crossed plan.
- 2026-08-15 durable exact-partition-capability invariant: a task-bound state partition may be authored only when the complete exact roster has the structured model or relation that can carry it. Warjack/Warbeast damage partitions use exact card type plus explicit piece flags; Payload `on_boundary` additionally requires an ending-side controller with a piece whose `controllerPieceKey` points to it. Absence is an exact candidate `proposal_filtered` result, never a Host reject, family-level unreachability or strategy conclusion.
- 2026-08-14 durable terminal-opening-identity invariant: a construction opening key identifies roster/formation geometry but is not a terminal-batch state identity because the same construction can be rebound to different Steamroller scenarios and Scenario Terrain states. Terminal execution must resolve an opening by a key that includes exact rosters, scenario, map, deployment seed, initiative and terrain-setup class, then match the strict opening-state and scenario-binding hashes. The current `39` task openings therefore have `39` terminal keys while retaining only `9` legacy construction keys as provenance.
- 2026-08-14 durable deployment-role invariant: Steamroller deployment depth is role-bound, not player-number-bound. The Attacker deploys from the official south 6-inch edge and the Defender from the official north 11-inch edge; when player2 is first player, those physical zones must be assigned to player2/player1 respectively. A legal geometry audit under the wrong role assignment is not a legal task opening.
- 2026-08-14 durable terminal-execution-batch invariant: a selected terminal task is completed only by a task-roster adapter bound to its exact representative and current Host/Construction receipts. Batch leasing may target only tasks with registered adapters. Missing adapters, finite execution budgets and unselected candidate mass remain `budget_deferred`; they cannot become Host rejects, proposal filters or unreachable claims. Opening success and generic capability pins never increment the task-specific strict-root count.

- 2026-08-14 durable custom-matchup-construction invariant: task-authored Warmachine roster universes must delegate cost, FA, attachment, battlegroup and modular option legality to the current formal Force Builder and bind its source into the construction Host receipt. Goal features include leaders, auto companions, fixed core/loadouts and flexible packages. A unit accepts at most one Command Attachment, three Weapon Attachments and one Leader Attachment; a Weapon Attachment replacing a Grunt changes host/attachment instance accounting without changing the combined physical-model total. Fixed-complete tasks skip roster search, while generated finite pools keep rejected, omitted, deduplicated and budget-deferred mass separate and never claim exhaustive faction coverage, reachability or win rate.

- 2026-08-13 durable Critical-Slow boundary: the parent rules Host owns exact current-data Critical Slow as an independently evaluated spray-hit status: a critically hit living model has base DEF 5 for one round. Toxic Spittle's Corrosion quality remains a damage type, not continuous Corrosion. The complete-roster Strygon terminal hypothesis has now been freshly materialized and independently replayed under that Host; it is selected execution evidence but not deployment-reachability or training evidence.

- 2026-08-13 durable score-terminal-piece-identity invariant: the seven two-Leader scoring anchors use exact Sepsira and Nymara card/model identities, stats, health, base and native resource projected from cited legal `100/100` fixed rosters. Each projected piece binds its complete source-piece and source-card hashes and declares omitted attack, feat, spell and special-action families. This proves only a turn-end scoring micro domain; it is not a legal two-model roster or a full-card tactical root.
- 2026-08-13 durable historical-turn-decision invariant: an inverse edge over `end_turn` must preserve the exact generic `actionPatch` used by the original transition. Public replay must execute previous-turn and score-terminal end turns through the composite replay path, otherwise scenario decisions such as Payload objective movement can silently disappear. All seven current score roots independently replay to their prior-turn frontier with the stored decision witnesses.
- 2026-08-13 durable current-terminal-ledger invariant: after Scenario Terrain setup and scenario-control relation refinement, the corpus has `737,841,700,800` subcells and the evidence-only selector retains `18,526` representatives. Current dispositions under an eight-task proposal budget are `21` strict-materialized, `14` current-Host strict-rejected, `17,436` budget-deferred, `8` proposed, `943` round-equivalence unresolved and `104` source-unresolved. Pending next dispositions are `49` expected Host rejects plus `17,395` ordinary strict candidates, and all mass is conserved.
- 2026-08-13 durable scenario-control evidence boundary: search may declare a control-relation hypothesis, but only `scoreScenarioElements` plus a strict settlement/replay can prove it. Current Pressure Point evidence proves both eligible two-side contest with no securing side and a model in range excluded by exact standard-building occupancy; each root has one exact terminal predecessor. The Host exposes `ineligibleModelsInRange` as audit evidence without changing scoring behavior. These later-round roots do not prove deployment reachability or strategy value.
- 2026-08-13 durable simultaneous-Leader-end-turn invariant: when an `end_turn` transition has already performed its single Steamroller settlement and that same settlement removes both Leaders, assassination resolution must reuse the completed settlement. It may not call the High Stakes fuse, scoring or scenario decisions a second time. The exact complete-roster High Stakes round-3 root changes `2:5` to `4:6` once and awards player2 the victory-point tiebreak; it remains an authored later-round hypothesis with no deployment reachability or training authority.
- 2026-08-13 durable simultaneous-Leader-action boundary: High Stakes objective blast and Strygon Toxic Spittle now certify two distinct simultaneous-removal causes. The Strygon root uses the complete `100/100`, `103`-model roster, an independent spray-secondary critical hit and two explicit boost declines; both one-box Leaders are removed, scoring changes `2:5` to `4:6`, player2 wins the VP tiebreak, and execution/replay receipts match. Other causes, other Scenario Presence configurations, unresolved ties and deployment reachability remain open.
- 2026-08-13 durable simultaneous-Leader-Scenario-Presence boundary: a second complete-roster High Stakes objective-blast subcell ends final scoring at `4:4`, then Host Scenario Presence resolves `0:6` for player2 from a complete Fane unit on the right 40mm objective (`2`) and Cylena on scenario terrain (`4`). Exact contributors and replay receipts are retained. VP plus Scenario Presence equality remains source-unresolved and cannot be promoted as a strict result.
- 2026-08-13 durable fixed-round source boundary: official Steamroller 2026 defines VP and Scenario Presence as common first/second tiebreakers and separately gives six scenarios a Defender-round-seven end. Decisive fixed-round outcomes are source-confirmed; only equality after both tiebreakers remains unresolved. Six complete-roster round-seven VP roots now pass Host static placement, `end_turn` terminal execution and independent replay. Two Fronts remains explicitly non-fixed.
- 2026-08-13 durable sealed-scenario-decision invariant: a validated `steamroller2026Decision` supplied on an attack must survive internal token/resource pre-roll windows by being copied into the locked source action. Explicit boost declines may not lose the High Stakes fuse choice or reopen an external decision. Arbitrary invocation metadata is not preserved.
- 2026-08-13 durable terminal-action-range invariant: being outside a direct weapon profile is not itself a rules rejection when the active model may legally move and then attack. Terminal corpus action-range cells distinguish direct-action range from movement-assisted reachability; only a current Host static/enumeration/execution receipt may classify an exact authored state as strict rejected.
- 2026-08-13 durable current-report-data invariant: a live remote refresh found version `40042` unchanged (`no_op`). Search-console media evidence binds the `40042` card snapshot, all `16` exact card portraits and all three official source archives by SHA-256. The fixed roster fixture remains sourced from reviewed `40041`; the two projected score-anchor cards were compared against `40042` and their executable card content is unchanged, while presentation still resolves only through exact current card UUID assets.
- 2026-08-13 durable official-replay-asset invariant: all seven Steamroller 2026 report backgrounds are exact local copies from the current official publication media archive and carry archive/member/image SHA-256 evidence. All printed objective/cache/flag-fallback centers use the official table-edge-to-base-edge measurements; rules coordinates use `x` west-to-east and `y` south-to-north, so canvas rendering must invert Y. Piece art requires exact piece/card identity plus an existing hash-verified file: the current fixed report has `103/103` identities and portraits, and all `16` card UUIDs / `103` model instances use report-local frozen assets traced to current `40042` official archives. The current-media gate checks data version/hash, three source archives, all archive members and all frozen copies. A stale path string is not an asset and the browser may not synthesize an unchecked fallback URL.
- 2026-08-13 durable Scenario-Terrain-setup invariant: a printed flag position is not always a separate terrain piece. At setup, an eligible ordinary terrain piece whose any portion is within 5 inches must be selected; only the no-candidate branch creates the 30mm obstruction fallback. The Host recomputes candidate keys by exact terrain geometry and Attacker/Defender setup order, removes terrain selected by earlier flags, and preserves both closest-geometry and center distances. The fixed Two Fronts `center_obstruction` has closest distance `0` and center distance `0.718`; a separate regression proves center distance `6` with closest distance `4.819` remains legal. Pressure Point now has one exact four-flag branch with two candidates per flag and nonfirst selections, strict terminal replay and exact inverse; remaining setup classes stay unresolved.
- 2026-08-13 durable archived-report-host-drift invariant: a persisted branch certified under an older Host receipt is historical evidence only. The console preserves its original disposition and replay receipt for inspection but republishes the effective branch as `rules_drift`, sets current strict count to zero, records both Host receipts and keeps training truth false. Only a fresh independent replay under the current Host may restore current `strict_certified` status.
- 2026-08-13 durable inactive-scenario-element contract invariant: Steamroller scenario contract validation uses the complete registered scenario-element roster even after a payload objective is delivered or a trench cache is scored and becomes inactive. Active-only views remain correct for scoring and interaction, but later turn settlements may not reject the scenario merely because a registered one-use element is inactive.
- 2026-08-13 durable official-Two-Fronts-correction invariant: replacing hand-authored coordinates invalidated the historical synthetic player1 `3:0` route; one intermediate official-coordinate cooperative route instead produced player2 `2:6` after 140 strict transitions. Neither historical result is reused. The current official-layout score report is a different round-3 position root: it independently strict-replays 49 structural actions, records left-50 scoring at player2 round-2, player1 round-3 and player2 round-3 settlements, and reaches player1 `3:0`; it is one existential path with `346` deferred inverse obligations, not a deployment or strategy-optimality proof.
- 2026-08-13 durable score-representative-refinement invariant: exact winner/opponent score-source multiset identity refines the score-terminal audit denominator without claiming that more continuous positions were executed. Current exact counts live in the current-score-ledger invariant above and are derived from the corpus rather than frozen in test assertions. The materialization ledger keeps every budget/source/expected-reject remainder explicit. Receipt-sensitive selection order remains a stability debt and is not a rules-coverage change.

- 2026-08-12 durable seven-scenario-terminal-corpus invariant: the Steamroller 2026 terminal corpus has seven scenarios, four terminal classes and `1,618` stable skeleton cells after scenario-specific mutable state and Kill Box are included. Its subcell count is a conserved, versioned audit denominator rather than an eager execution queue; score-source refinement superseded the original `15,375,441,600` count. Six scenarios use the official round-7 limit, while Two Fronts has no fixed round and retains an explicit `8+` symbolic tail.
- 2026-08-12 durable cross-scenario-score-root invariant: one real `end_turn` score-terminal root for each Steamroller 2026 scenario independently enumerates, executes and replays under the current Host, then reverses to exactly the strict enumeration-prepared predecessor with `1` candidate, `0` reject and `0` unresolved per scenario. Authored input, normalized action input and enumeration-prepared hashes remain distinct. High Stakes additionally has exact inverse scopes for empty detonation, damage-only, target-only lethal lifecycle and multiple damage targets; its exact branch probability multiplies the `d3` fuse and every `d6` damage roll. Any lifecycle mutation outside the damaged target, such as a destroyed Leader changing a controlled warbeast, remains unresolved and stops before candidate publication.
- 2026-08-13 durable terminal-corpus-rules-authority invariant: reverse search presets a complete rules state and uses the interaction graph only to choose coverage samples. It does not implement Stealth, True Sight, LOS, control or other game rules a second time. Only Project D rules-v1 Host static audit or strict action replay may publish `strict_rejected`; malformed inputs are `input_invalid`, Host-legal states that miss a declared search relation are `proposal_filtered` after replay, and unexecuted work is `budget_deferred`. Accepted roots remain non-training hypotheses until reverse reachability and the current ruleset snapshot gates pass.
- 2026-08-13 durable score-materialization-disposition invariant: a valid Host transition that fails to produce the requested terminal, score-source identity or representative partition is `proposal_filtered`, not a rules rejection. A roster/history/corpus identity defect is `input_invalid`. The first executed expected-reject batch contains twelve exact score representatives across the six fixed-round scenarios: six loser-Leader bases outside the table and six loser-Leader bases overlapping exact blocking terrain. Every row is rejected by the Host static-placement audit; none claims historical reachability or training truth.
- 2026-08-13 durable terminal-corpus-roster-scope invariant: a structural terminal class and a concrete execution roster witness are distinct scopes. The current two-Leader score projection explicitly omits attacks, spells and feats; it can prove turn-end score transitions and pre-action geometry rejects but cannot certify assassination or simultaneous-removal action representatives. Concrete assassination and simultaneous-removal evidence must bind a full-card roster witness or an audited existential-roster contract; the first High Stakes simultaneous root now does so for one exact subcell only. Search may not invent an attacker to satisfy the corpus.
- 2026-08-12 durable terminal-representative-selection invariant: the corpus is reduced without Cartesian enumeration by a deterministic evidence-only selector. Its first strength-1 plan covered every declared skeleton and selected-cell partition-value obligation with baseline/single-dimension representatives; later source refinement may change exact counts while unselected mass remains explicit. Representative selection is not strict certification, reachability, strategy value or cross-dimension coverage, and source/unbounded/expected-reject rows retain separate next dispositions.

- 2026-08-13 durable terminal-anchor-coverage invariant: search-console coverage is rebuilt from current-Host strict execution, not a hand-authored completed count. Seven score-terminal anchor proposals execute and independently replay on startup, then their exact subcell dispositions are projected into the current corpus. The evidence binds source/assessed corpus hashes and Host receipt; it proves only those seven exact score-terminal subcells, not reverse-to-deployment reachability, strategy quality or corpus completeness.
- 2026-08-13 durable player-report invariant: the player-facing report may translate strict evidence into Chinese result, route, risk and step summaries, but `[0,1]` or otherwise uninformative probability remains `待估` and never becomes a natural win-rate claim. Previous/next/play, full-board/action-focus viewing and same-state branch switching must preserve the underlying node/edge/receipt identities; presentation ordering cannot alter search inclusion or strategy truth.
- 2026-08-13 durable replay-presentation-evidence invariant: a replay may render a map bitmap only when the search state binds that exact asset; a synthetic position root without such a binding renders board, terrain and scenario elements from its exact rules-state geometry and says so. Piece art is resolved only by exact piece identity and card UUID from the same result/checkpoint lineage. Fuzzy name or substring matching is forbidden. A card whose source `portraitPath` is empty may use an official media-bundle asset only through an explicit card-ID map carrying archive, member and SHA-256 evidence; otherwise the UI displays an identified fallback token and a visible asset gap. Presentation assets never grant Rules, reachability, strategy or training authority.
- 2026-08-13 durable representative-materialization-ledger invariant: executed evidence is pinned into, not merely displayed beside, the selected representative denominator. Final outcomes require exact cell/subcell, Host and roster receipts; proposed and budget-deferred tasks are never counted as execution. Source-unresolved and unbounded-round rows remain separate, and selected plus unselected mass must equal the versioned corpus denominator. The seven score anchors prove only the explicit two-Leader micro-roster domain; current counts live in the current-score-ledger invariant.

- 2026-08-12 durable interactive-report invariant: the victory-path workbench may filter and order discovered branches only by auditable evidence dimensions such as strict replay, deployment reach, reverse depth and action count. These are view controls, not strategy scores or optimality claims. Each branch publishes action composition, key actions, score/damage/resource/removal deltas, probability interval, action-space closure and strict receipt counts; the terminal publishes per-assumption provenance. Preset seed scenario/goal/anchor identity is server-audited, and a same-budget resume republishes its checkpoint candidate after independent strict replay instead of hiding an already-known route.

- 2026-08-12 durable Ticket-16-multi-turn invariant: the fixed 103-model assassination and score terminal roots each have one concrete candidate reversed through two complete prior player turns and independently replayed under current rules-v1 strict with zero rejected branches. The assassination reaches player1 round-2 Control at `0:0`; the score route preserves its `1:0` first prior-turn frontier and continues to player2 round-2 Control. These are existential bounded pass histories, not deployment witnesses, exact assassination probability, alternate-order exhaustion or cross-scenario coverage.
- 2026-08-12 durable reverse-state-identity invariant: missing models, remaining boxes, warjack systems, warbeast branches, removed-from-play, reserve and disabled lifecycle all participate in semantic state identity. Equal clean rules states may share expansion work, but different activation-order histories retain separate labels with cumulative probability/interval, resource envelope, control residue, score, adversarial context and provenance.
- 2026-08-12 durable reverse-resume/disposition invariant: terminal-rooted search emits a Host-receipt and terminal-cell-bound runtime checkpoint for every retained budget frontier. A micro route proves stop-at-zero-turn, persist four frontiers, resume without root-prefix replay, reach deployment and full strict replay. Reports use one canonical seven-kind disposition protocol; only `proven_unreachable` counts as a proved failure, while rejected/deferred/pruned/unresolved/drift remain separately recoverable or invalidated.
- 2026-08-12 durable Ticket-16-closure invariant: Ticket 16 is resolved after both fixed 103-model terminal families strict-replayed two complete prior player turns, all state/history identity gates passed, and the console resumed one persisted 103-model frontier under a larger budget with `resumeAccepted=true`. `current_turn` and `long_horizon` are explicit semantic modes; reverse-turn/label budgets may increase within a mode without changing the checkpoint contract, while a semantic mode change invalidates and rebuilds rather than reusing the old frontier.

- 2026-08-12 durable search-publication invariant: a human-facing reverse-search result is an append-only, versioned session over typed commands and events, not a mutable rules state. The console may display discovered strict reachability witnesses, board timelines and branch comparisons, but it must simultaneously retain strict rejects, low-probability unexpanded mass, budget deferrals, inverse gaps, rules unknowns and proven-unreachable rows. A green route is not a strategy score, natural win rate or global optimum.
- 2026-08-12 durable injected-seed/training invariant: preset and exact terminal roots supplied through the console carry `user_injected` provenance and `trainingTruth=false`. Exact roots must fail closed on Host receipt, strict-state, static-placement, terminal-inverse or oracle-isolation mismatch. Reachability training export requires a legal deployment endpoint, fresh independent whole-route strict replay, per-assumption strict evidence, conserved probability closure and complete opponent-response closure; any failed gate emits zero candidates and never turns rejected/unresolved evidence into negative examples.
- 2026-08-12 durable search-console recovery invariant: interactive search runs in an isolated worker controlled by typed start/pause/resume/cancel commands and persisted SSE events. Server restart converts a formerly running or paused session to explicit `interrupted` with stale PID cleared. Cached terminal roots are keyed by current Host receipt plus terminal seed identity, so rules drift cannot silently reuse an old root.

- 2026-08-12 durable fixed-score-frontier invariant: the fixed `100/100`, `103`-model Cryx/Fane Two Fronts score corpus generates four explicit round-3 roots from scenario/control relations without reading opening or deployment coordinates. One strict root reproduces `2:0 + 1:0 = 3:0` on the declared `left-50`, then reverses through the complete fixed-roster segment to player1 round-3 Control at `1:0`; fresh rules-v1 replay certifies every edge. The six player1 Raptors retain Power Up focus through player2's turn and lose it only when terminal `end_turn` switches to player1 and applies next-side maintenance cleanup. Pass inversion must preserve activation-end continuations: this route contains seven explicit `decline_reposition` decisions from six Raptors plus Akulon.

- 2026-08-12 durable Ticket-09-closure invariant: the fixed assassination and score families each have one oracle-isolated, strict-materialized round-3 position root reversed through one complete prior player turn and independently strict-replayed, so Ticket 09 is resolved. This does not discharge the three deferred alternate roots per family, exact assassination Chance, 24 other terminal action families, pre-existing controller/upkeep inverses, cross-turn deployment reachability, parameterized seven-scenario coverage or cross-roster validation; those remain Ticket 16/17/18 obligations.

- 2026-08-12 durable fixed-terminal-frontier invariant: the first `100/100`, `103`-model Cryx/Fane round-3 assassination position root now has a concrete strict reverse segment through one complete prior player turn. The `(18,21)` root derives player2's turn-2 `0:3` settlement and player1's current Control/Power Up by strict execution, then reverses to player2 turn-2 Control at `0:0` through 15 activation groups; an independent fresh replay closes every edge through the killing attack and Life Drinker. This is one existential pass-history witness with probability `[0,1]`, not a deployment route, alternate-order exhaustion, score-terminal result or multi-position result.

- 2026-08-12 durable later-round-temporal-root invariant: a later-round position root must derive its immediately preceding turn settlement and current-side Control Phase by strict rules execution. Manually changing `phaseKey` from Control to Activation can omit Power Up, frenzy/upkeep/resource effects and create an unreachable root. A fixed materialization budget selects roots by explicit semantic anchor identity, never by cell-hash order.

- 2026-08-12 durable focus-control-envelope invariant: a later-round activation-start root must include resource values reachable from the preceding Control Phase. `reverse_focus_control_baseline` restores an active warcaster to pre-replenishment zero, each active warjack to one less than its target Power Up value, and preserves focus on models that do not automatically replenish; target-guided strict replay may allocate only when the target activation state requires it. Static opening resource values are not automatically valid later-round temporal assumptions.

- 2026-08-12 durable terminal-position-domain invariant: a later-round terminal position domain is generated and audited before reverse search; it is never a deployment template. Board/scenario anchors, action-range and Leader-control relation bands, finite directions and later LOS/topology partitions produce explicit coordinates for every live model without reading opening positions. Each strict-materialized cell is searched independently, with generation/materialization/search reject and defer entries conserved in one denominator. Deployment geometry appears only in the far endpoint audit, and static blocking-terrain placement uses the read-only rules-v1 Host audit.

- 2026-08-12 durable control-resource-predecessor invariant: a nontrivial control predecessor restores an explicit earlier resource envelope and must strictly replay maintenance, replenishment/leeching, Power Up, allocation and control close to the supplied activation state. Full-route replay freshly selects the candidate's stored control action identities. `focusSpentThisActivation` is an activation-only rules-v1 tally; control allocation cannot seed it.

- 2026-08-12 durable benchmark-scope invariant: Ticket 09 is a fixed Cryx/Fane vertical benchmark, not evidence of general multi-turn or all-scenario completeness. Project completion additionally requires cross-turn recursive reverse search, a finite parameterized Steamroller 2026 terminal corpus, and cross-scenario/roster batch validation. Fixed-scenario success may not discharge those separate coverage obligations.
- 2026-08-13 durable terminal-roster-witness invariant: structural seven-scenario terminal cells and concrete rules execution use separate evidence. A complete execution witness binds exact legal rosters, card-backed model identities, points and deployment provenance; it supplies models to an authored later-round state but makes no terminal-rule or reachability conclusion. The rules-v1 Host must still audit placement, enumerate and execute every terminal action and independently replay it. A two-Leader score-only projection that omits attack/spell/feat families may never be reused to invent an attacker for assassination or simultaneous-Leader removal.

- 2026-08-12 durable historical-damage invariant: complete-roster provenance does not mean every predecessor model was alive and undamaged. Cross-turn reverse state identity includes relevant pre-existing casualties, remaining damage tracks, warjack systems, warbeast aspects, disabled capabilities, replacement/removal/reserve lifecycle and their source history. Budget-deferred, low-probability-pruned, inverse/rules-unresolved and strict-rejected branches retain distinct mass, recovery and invalidation semantics; none may be silently converted to failure.

- 2026-08-12 durable terminal-rooted-benchmark invariant: a route constructed from legal deployment by forward policy execution is a strict oracle, not a reverse-search result. Ticket 09 may close only when assassination and scoring searches start from finite versioned round-3 position roots, receive no opening/action/intermediate-state oracle input, materialize concrete predecessor states and effect-to-precondition edges through at least one complete prior player turn, and independently strict-replay each discovered segment forward. Continuing across multiple turns to legal deployment is Ticket 16; deployment never supplies round-3 root geometry.

- 2026-08-12 durable terminal-hypothesis invariant: reverse search begins with a finite terminal hypothesis cell, not an arbitrary exact future trace. A cell may assume round, ending side, victory type, causal action/settlement family, relevant models, pre-terminal damage/resources and rule-relevant geometric relations; every assumption is typed as rule-derived, user-constrained, optimistic proposal or randomized proposal and remains non-truth until strict certification. Continuous coordinates are first quotiented into rule-behavior relation cells and only then materialized and checked for path, collision, LOS and legal landing. Existing forward policy/Chance/AND-min machinery is a local milestone connector and verifier, never the generator of the whole route from deployment.

- 2026-08-12 durable reverse-preimage-state invariant: a concrete inverse edge targets the deterministic rules-v1 enumeration-prepared state actually consumed by strict apply, not an earlier hand-authored input that lacks default profiles, mapped terrain or completed pregame choices. The authored input hash, enumeration-prepared hash and successor hash remain distinct evidence. A candidate is a strict preimage only when independently enumerating/applying its action, Chance class and owned response exactly reproduces the supplied successor semantic state and requested terminal event; matching only the event or only a relaxed state projection is insufficient.

- 2026-08-12 durable score-settlement-preimage invariant: a scenario-score terminal hypothesis carries exact pre-settlement score and score-gain maps in addition to human-readable relation keys. The inverse restores the ending side, absolute round, activation phase, reset activation markers and only the current settlement's ledger rows; strict `end_turn` must reproduce the complete supplied successor, requested score terminal and side/round transition. Scenario element points and Kill Box are rules-derived during certification, never inferred from a generic “scores one” label.

- 2026-08-12 durable stochastic-policy invariant: a caller-fixed multi-action policy is evaluated as a stochastic game, not a flat probability tree. Exact dice classes are Chance-weighted; damage transfer and every other legal owned response are selected by their owner, with opponent choices aggregated by adversarial AND/min and acting-side choices by max. Defender-owned compound variants are never acting-side candidates. Every executed response edge retains a strict receipt, same-depth equal canonical state/cursor pairs may share conditional evaluation, and incomplete response sets or unsupported successor effects widen the probability interval rather than disappearing. Caller-declared deterministic actions still require strict execution, no attack-resolution payload and no opponent reaction requirement. Nonzero low-probability pruning remains disabled until same-depth merging is proven within adversarial decision context.

- 2026-08-12 durable adversarial-Chance equivalence invariant: exact Chance classes may share one continuation only after strict execution proves an identical complete owner-aware response vector: same response owner/kind/completeness, response identities, acceptance or rejection, canonical successor state, terminal classification and terminal events. Receipt hashes and original roll classes are evidence rather than semantic identity, so they do not block reuse but must all remain attached. Equality of attacker-side results alone is insufficient. This local equivalence does not authorize nonzero probability pruning; cross-parent same-layer mass still requires an adversarial-context frontier ledger before thresholding.

- 2026-08-12 durable frontier-threshold invariant: nonzero probability thresholding is valid only after a full same-layer merge keyed by canonical rules state, policy cursor, terminal/continuation suffix and adversarial decision context. Owner-response alternatives receive different contexts and never add probability; deterministic singleton transitions preserve context. Known terminal or unresolved suffixes bypass the threshold. Raw route-ledger mass can exceed one because it inventories mutually exclusive owner choices, while each lower/upper selected adversarial policy ledger must separately conserve `success + failure + pruned + unresolved = 1`.

- 2026-08-12 durable frontier-recovery invariant: every resumable frontier label is persisted separately from its content-addressed canonical state, ancestor/child edge, original Chance contribution and full strict receipt. Multiple adversarial labels may reference one state, so resumable-label count and unique frontier-root count are different metrics. A restored continuation must start at the original absolute depth, policy cursor, adversarial context, continuation suffix and exact cumulative mass, reproduce the same semantic label key, and may then execute without replaying the root prefix. Continuation reports remain immutable versions until an explicit suffix-stitch operation recomputes the ancestor AND/min value.

- 2026-08-12 durable frontier-stitch invariant: a resumed report may replace only the exactly matching budget-deferred semantic suffix. Same-depth descendants from separate resumed roots may converge only with equal state, cursor, continuation and adversarial context; their distinct incoming edges and exact rational contributions are then unioned once. Different owner contexts never merge, nonzero threshold batches with multiple roots fail closed until a batch-wide layer merge exists, and all unselected suffixes remain unresolved. Each committed generation content-addresses the complete report snapshot plus state/receipt references, so a new process can restore the ancestor report/runtime pair and continue without replaying the prefix or mutating prior checkpoints.

- 2026-08-12 durable parallel-continuation invariant: parallel workers receive disjoint, already-bound immutable frontier labels plus a local content-addressable policy factory configuration. Workers may execute strict suffixes but never write the external DAG. The parent requires every selected task to return a label/task-bound terminal message, rejects timeout, transport, early-exit and policy-load failures, performs one canonical stitch only after the whole selected batch succeeds, and leaves publication to one later checkpoint writer. Parallel and sequential execution of the same batch must have identical report and runtime-checkpoint hashes; timing metadata is audit evidence, not search semantics.

- 2026-08-12 durable incremental-frontier-storage invariant: external generations reuse only content references returned by a verified same-process restore. Canonical states and strict receipts reuse hash-bound objects; route-label objects contain immutable semantic identity only, while incoming mass, solve status, interval and expansion remain in the content-addressed report snapshot and checkpoint ledger. Edges, Chance evidence and unresolved records therefore reuse unchanged parent objects. A bounded lazy restore may materialize only the canonically selected frontier states and keep all other old state/receipt bodies behind verified references; every selected state and every new object is still fully checked before execution or publication.

- 2026-08-12 durable continuation-supervisor invariant: a supervised continuation batch binds ancestor report/runtime hashes, canonical selected labels, continuation budgets, worker-policy module/factory/config hashes and caller dependency hashes into one run key. A single live PID owns the status lease. Parent-observed spawn/start/liveness/terminal events update an atomic hash-bound status; they prove process liveness, not progress inside a synchronous rules action. A dead owner leaves an interrupted running record and stale lease; the next process may take over the same run key only after confirming the old PID is dead, increments the attempt number, retains the prior summary and recomputes all unpublished labels from the immutable ancestor.

- 2026-08-12 durable assassination-checkpoint invariant: the fixed Sepsira/Cryx versus Fane route has a strict checkpoint after 54 deterministic transitions, inside Sepsira's still-open activation and immediately before the first stochastic action. Its next action is boosted channeled Excarnate through the Raptor. The exact cursor closes all 17 dice classes and defender responses because Excarnate can deal at most 11 damage to Nymara's current 24 boxes, so its boxed-only lifecycle hook is provably unreachable in this state; the same hook remains unresolved whenever maximum damage can box the target. The continuation after Excarnate remains `[0,1]` depth-budget unresolved, and the prior maximum-roll terminal route is only an existential witness.

- 2026-08-11 durable update-detection invariant: a Host dependency receipt hashes the recursive closure of local static module imports, not only public entry modules. Any nested executor, rule atom, adapter or scenario dependency drift changes the composite authority receipt and enters candidate impact review. Promotion requires an evidence-bound review receipt and regenerates the reviewed baseline and semantic index from the same current Host; copying candidate files or accepting a version string is not promotion.

- 2026-08-11 durable normalization/transition-receipt invariant: deriving executable rule evidence must be idempotent. Re-normalizing a state may not concatenate prior normalized source evidence or change its canonical identity. A strict route receipt separately binds the normalized action input, the deterministic enumeration-prepared state and the normalized transition result; chain validation compares the prior result to the next input while retaining the enumeration mutation as auditable evidence.

- 2026-08-11 durable ruleset-maintenance invariant: a Warmachine update is reviewed as a composite semantic change, not accepted by version string or raw file hash alone. The reviewed index binds per-card rules/construction identities, described rule sources, behavior tokens, atoms, hook families, review-only interaction neighborhoods, Steamroller profiles and strict Host source hashes. Exact source fingerprints may propose atom reuse but never authorize it; a source without an explicit atom mapping may still be owned by core/generic strict execution, so it requires execution classification and positive/negative strict scenes rather than an automatic “unsupported” or “supported” claim. New semantics fail closed. Core, timing, scenario, atom or hook drift invalidates the corresponding checkpoint/witness/coverage/skill claims; construction-only drift rebuilds roster pools and legal openings. Candidate indexes never overwrite the reviewed baseline before graph, replay, benchmark and ctx2skill/skill2ctx gates pass.

- 2026-08-10 durable upstream-authority invariant: this repository owns reverse-search algorithms but never Warmachine legality or transition truth. It loads the Project D Warmachine host through a narrow adapter; only upstream rules-v1 enumeration/application with strict RNG and reaction materialization can certify a route. Every result binds Git revision, dirty state and critical source hashes because an uncommitted upstream cannot be identified by commit alone.

- 2026-08-10 durable result-claim invariant: terminal constraints, symbolic predecessors, opening/frontier compatibility, strict forward witnesses, adversarial evaluations and construction guidance are distinct evidence levels. The reverse product reports discovered reachability candidates and does not rank strategy; downstream comparison may rank only certified routes and does not claim that ungenerated routes contain no better strategy. Missing terminal classes, predecessor operators, opponent responses, chance mass, legal openings or deployment proposals remain unresolved and may not be silently treated as losses.

- 2026-08-11 durable pure-reachability invariant: reverse search answers only whether and how a predecessor can reach a declared successor under rules-v1 strict execution. Holding, blocking, exchanging, withdrawing, scoring and attacking are peer successor cases. Tactical utility, material swing, scenario pressure, LLM/Skill preference and deliberate intent may exist only as downstream annotations; they cannot affect reverse inclusion, ordering, rejection or hard pruning. Candidate order is canonical identity, and every budget-deferred candidate remains unresolved.

- 2026-08-10 durable interaction-graph invariant: Project D's manually curated source/code/test evidence graph and its rule-atom interaction export are different products. Reverse search must derive a typed semantic graph and attach evidence as an overlay; untyped normalized keys, shared hook names, and lifecycle words may order or audit work but may not create transitive dependencies or justify hard pruning.

- 2026-08-10 durable temporal/provenance invariant: reverse search starts from a minimal Steamroller assassination or score certificate, records both victory-relative and absolute round timing, and returns through complete legal deployment to legal construction. Every model needs legal provenance and remains in the complete-roster lifecycle state after destruction, boxing, removal, dormancy, or replacement. Deathclock is outside strategy-search scope.

- 2026-08-10 durable state-identity invariant: static rules receipts, canonical rules state, search context, route labels, and explanation/training provenance are separate records, exposed through separate rules-state, search-memo, and route-label identities. Only exact state identity or a behavior-preserving quotient with evidence may hard-merge; formation, topology, distance, or geometric similarity may only order proposals.

- 2026-08-10 durable reverse-convergence invariant: when different terminal-conditioned reverse branches materialize the same complete canonical rules state, they may share lower-level predecessor computation. Their state-to-terminal proof suffixes, probability mass, path costs, exchanges, intent and training provenance remain separate labels, and every suffix requires its own rules-v1 strict forward certification.

- 2026-08-10 durable search-pipeline invariant: reverse proposal generation, rules-v1 strict forward certification, and optional forward completion are separate evidence-producing stages. Current-turn and long-horizon search are separate modes. Whether milestone-based forward completion is useful remains an experiment, not an architectural assumption.

- 2026-08-10 durable Steamroller 2026 invariant: scenario scoring begins at the end of the Defender's second turn; scenario victory requires a player to lead by at least 3 VP after scoring on that player's opponent's turn. Assassination occurs when one player owns the only Leader model remaining; simultaneous removal of all Leaders uses tiebreakers, and final VP after assassination cannot replace the assassination result. The current Host's absolute-score threshold and single-Leader-removal terminal helpers are not sufficient evidence for a 2026 terminal witness.

- 2026-08-11 durable Steamroller Host-parity invariant: the prior Host insufficiency is resolved by the Project D seven-profile Steamroller 2026 state machine. A reverse-search terminal witness may bind this Host only when the source/Host combined gate is green and the state contains the exact scenario roster, geometry, ownership, mutable state, timing, Leader roster and required controller decisions; an unknown or incomplete branch remains rejected/pending. Layer3's persisted scenario state and `steamroller_2026_turn_end_settled` receipt are part of the Host boundary, and legacy Layer3 scoring may not add VP after that receipt.

- 2026-08-11 durable external-storage invariant: the persistent search graph separates content-addressed canonical rules states, strict edges, route labels, Chance contributions, receipts and checkpoint roots. State convergence may share computation and payloads but never erases terminal suffixes, costs, deliberate exchanges, intent, provenance, unresolved mass or probability-event identity. Digest hits require type/schema/receipt/length and full canonical-byte verification.

- 2026-08-11 durable external-execution invariant: large reverse expansion uses RAM only for the hot frontier and merge buffers, while immutable candidate segments are partitioned and exact-merged into a cold DAG before a transactional checkpoint acknowledges work. Bloom filters, bounded transposition tables, frontier-only storage and per-state single-score retention may accelerate ordering but may not hard-dedupe or support completeness claims.

- 2026-08-11 durable checkpoint/recovery invariant: worker input is acknowledged only after an immutable merge manifest and checkpoint are published through an atomic CURRENT pointer. The current root binds Host receipt plus local source/config hashes, exact-index generations, current input segments, frontier and in-flight/unresolved ledgers. A killed worker leaves only an ignored temporary segment; a killed merger may leave a resumable outbox and stale single-writer lock. Missing or corrupt dependencies freeze the store. Stable acknowledgments bound checkpoint batch history instead of growing every root forever.

- 2026-08-11 durable storage-budget/GC invariant: RAM, disk, time, record and label limits stop candidate generation by creating independent unresolved records and preserving omitted mass. GC marks CURRENT and report/training pins, waits two completed checkpoint generations by default, then uses quarantine plus COMMITTED recovery semantics; it may never reinterpret or sweep an active root. The first 106-model three-layer calibration produced V/G/E/L/C/U 9/15/10/10/10/3, six delta states, 1.54 MB final storage, 1.0278x physical write amplification and a 26.66x bounded-working-set reduction versus route-tree materialization; these are calibration evidence, not scale or optimality guarantees.

- 2026-08-11 durable typed-graph invariant: the canonical capability graph is generated from a Host receipt that directly hashes rules-v1, the Layer3 adapter, atoms, exact probability and Steamroller 2026 profiles. Structured operator facts and Host core facts may answer terminal capability queries; evidence surfaces, shared hook names, normalized declaration strings and generic constraints have no semantic transitive authority. Data drift changes the graph receipt/hash, while unknown semantics remain explicit constraints or wildcard sources.

- 2026-08-11 durable roster-projection invariant: a matchup source denominator separates exact source-ID/explicit-atom mappings, Host core-rule facts, structured geometry/attack facts and unresolved wildcard sources. The current fixed 106-model projection is 102 unique card sources over 351 occurrences, 38 exact sources mapping 37 atoms, 64 unresolved sources, plus 24 core/structured facts for 126 classified entries. Text similarity never upgrades a source to exact, and an unresolved source keeps every owning model relevant.

- 2026-08-11 durable capability-bottleneck invariant: a terminal obligation may use exact Host, atom/operator, core-fact and wildcard providers. A unique exact model or rule is an ordering lock only; wildcard providers, core capabilities, incomplete opponent/reaction closure, generic operator constraints or insufficient held-out strict traces disable hard branch locking. Terminal query ordering must preserve the complete input action identity set.

- 2026-08-11 durable terminal-proof invariant: assassination and scenario scoring are disjoint Steamroller 2026 proof types. A certificate binds an exact scenario profile, the requested winner, the Host terminal winner and the strict settlement event chain. Assassination additionally binds the complete Leader roster and final-VP precedence; scoring additionally binds the Defender-second-turn start, settlement on the winner's opponent turn and a lead of at least 3 VP. A desired terminal is never training truth until an exact rules-v1 strict replay emits the matching terminal.

- 2026-08-11 durable predecessor-algebra invariant: reverse obligations use actual AND, controlled OR, adversarial, Chance, trigger/order, reaction and strict-witness nodes across victory-event, activation, turn, previous-turn and previous-round layers. Unknown inverse transitions, provider semantics, opponent responses, probability mass, trigger orders and reactions remain independent unresolved records. A strict concrete route does not imply opponent robustness, complete chance mass, hard pruning or global optimality.

- 2026-08-11 durable reverse-roster/exchange invariant: terminal search keeps every model in complete roster provenance through disabled, boxed, destroyed, removed, dormant and replacement stages. Strategic exchanges record grouped removal points, score and resource deltas, while deliberate intent remains route provenance only. The external DAG stores canonical rules states as state records, algebra/proof suffixes as route labels and unknown obligations as unresolved records.

- 2026-08-11 durable downstream-efficiency-policy invariant: route quality analysis remains a multidimensional interval vector after reachability certification; no scalar score becomes rules or training truth. This optional downstream policy cannot alter reverse candidate inclusion or disposition. Exact Host/capability/admissible-bound refutations may prove reachability impossibility, while every finite-budget omission remains unresolved.

- 2026-08-11 durable Pareto-safety invariant: hard route-label suppression requires the same complete state/terminal/search context, strict terminal witnesses, exact hard dimensions, extension-monotonicity evidence and no independent Chance contribution that would be lost. Missing evidence, unknown dimensions, computation cost, unresolved mass, spatial slack, redundancy, temporary material/score loss and deliberate exchange are ordering or scheduling signals only.

- 2026-08-11 durable mode invariant: current-turn reverse search has a zero-turn horizon and requires exhausted relevant legal actions, opponent responses, Chance mass and trigger/reaction closure for complete reachability coverage. Long-horizon reverse search uses bounded iterative deepening and may report strict concrete reachable routes while uncertainty remains; no such route is a strategy ranking, exact strategy proof or global optimum.

- 2026-08-11 durable forward-completion invariant: exact reverse atomic terminal suffixes are strict-replayed first and bypass forward completion when already certified. A bounded milestone-guided strict connector may fill only an explicit gap between a complete rules state and a typed reverse milestone or terminal; unguided forward search remains a benchmark. Unavailable actions refine missing predecessor obligations, exact strict rejects may refine only their bound state/action, and every budget stop or omitted legal action remains unresolved rather than proving impossibility.

- 2026-08-11 durable construction-pool invariant: fixed-faction reverse search consumes a content-bound finite Force Builder proposal pool, not an assumed distribution over every legal roster. Canonical roster identities omit ephemeral entry IDs but preserve faction, Leader, cards, points, model counts, modules, attachments and battlegroups. Duplicate aliases share computation and accumulate source mass; invalid, terminal-excluded, selected and budget-unresolved mass remain separate and conserved.

- 2026-08-11 durable nested-opening invariant: legal opening mass is nested through roster pairs, declared first-player alternatives and finite formation-archetype pairs. Exact coordinates and deployment legality remain owned by the construction Host. A returned legal opening does not imply construction-space or continuous-deployment completeness; every omitted roster, pair or formation remains unresolved mass.

- 2026-08-11 durable model-history invariant: every model retains deployment source and lifecycle identity in every complete-state checkpoint. Directly terminal-relevant models require a declared complete history scope and a current-Host, action-bound, before/after-state-bound strict receipt for every segment. Deferred models retain individual identities and conservative reachable envelopes; incomplete special-movement closure may only order expansion. Capability bottlenecks hard-exclude a roster from one terminal proposal only with closed sources and alternatives, exact optimistic bounds, strict preservation evidence and no wildcard.

- 2026-08-14 durable completion-gate invariant: project completion is one inventory-checked command over every focused verifier, not a hand-selected passing subset. The current baseline is `remote-40041-v5`; Host receipt `80ce324e...` passes 8/8 gate groups and 79/79 verifiers. This authorizes bounded, recoverable and auditable reverse-search research only. Reverse search cannot affect rules, unresolved/deferred mass remains public, training export stays fail-closed, and continuous-state exhaustion, natural win rates, strategy ranking and global/faction optimality remain explicit nonclaims.

- 2026-08-14 durable trample/Reposition invariant: an ordinary Trample power attack is neither a run nor a failed charge. After its movement, attacks and interrupts resolve, a model with Reposition reaches the activation-end Reposition choice and completes activation only after moving or declining. Frenzy's immediate activation end is a separate exclusion. All Reposition movement still uses Host-owned path, collision, landing, cannot-advance, reaction and disengagement validation.

- 2026-08-14 durable activation-completion harness invariant: a caller's first-step movement-path enumeration scope applies only to the requested movement choice. Automatic post-move activation completion must clear that scope while a runtime window is active, or use only timing actions when no runtime window exists; leaking `explicit_path` into continuation enumeration can hide legal pass/end-window actions and falsify reverse reachability.

- 2026-08-14 durable Frenzy-activation invariant: Frenzy is resolved in the Control Phase but immediately activates the warbeast. Host predicates for effects that apply "during this model's activation" must therefore be action-aware and treat Frenzy charge/lost-activation sequences as activations; a phase-only `phaseKey === activation` test incorrectly suppresses boxed/destroyed and attack hooks such as Feast. In the current Vordak proof, the first Frenzy attack opens and settles Feast before Rampant Fury can start the mandatory second Frenzy.

- 2026-08-14 durable rule-gap feedback invariant: a reverse-search gap receipt binds the exact source rule fingerprint, current Host receipt, affected atom-definition hashes and interaction-closure hash. Any of those changes invalidates dependent terminal roots, reverse routes and value reports for rebuild. A repaired minimal replay may change `rules_unknown` to `strict_transition_accepted`; unresolved source drift stays `strict_rejected`, and neither disposition grants search-side rule semantics or training truth.

- 2026-08-22 durable reverse-resource-continuity invariant: an activation predecessor cannot copy an activation-end resource value backward when the immediately preceding control phase must replenish or leech a different value. The inverse must restore a concrete pre-activation resource state and a complete Host-enumerated action prefix that spends or gains the difference; every prefix action, explicit Chance outcome, continuation window and following movement/action is strict-executed, and the sanitized result must exactly equal the supplied successor. A synthetic zero-resource predecessor, search-side resource deletion, or control-phase approximation is not a reachability witness. Unsupported resource sinks, targets, action families and budget omissions remain unresolved.

- 2026-08-22 durable reverse-movement-budget invariant: deployment-origin budgets are applied after legal-placement candidates are ordered ahead of occupied/out-of-bounds candidates. Paid-run resource preimages are proposal ordering only: Warjack run tries `successor Focus + 1` first and Warbeast run tries `successor Fury - 1` first, while Host strict execution and exact successor equality remain the only acceptance authority. An explicitly bounded search may stop after a finite accepted-candidate or Host-attempt count, but every unexecuted origin, path and resource preimage remains unresolved; an exhausted budget is never a rules rejection or impossibility proof, and the default movement predecessor remains exhaustive.

- 2026-08-22 durable historical-layer parallel invariant: terminal-rooted historical parallelism splits only checkpoints whose frontiers share one reverse depth and expands exactly one prior-turn layer per batch. Each child receives one source/Host/options-audited frontier and never writes shared state. The parent validates task/label binding, requires every child to finish, checks merged label/state/edge identity, and only then publishes one checkpoint; timeout, transport, no-progress or any child failure leaves the batch unpublished. Worker timing, liveness and activation progress are audit evidence and do not alter search semantics.

- 2026-08-25 durable Warmachine transition-refinement boundary: a strict geometry witness records a successor semantic-state hash and ordered Host event sequence. Two different observation signatures inside one endpoint/predicate cell are a sound counterexample to transition stability and force refinement. Equal signatures over any finite witness set are only observations; they do not prove connectedness, path-class completeness, alternating transition stability or behavior equivalence. A CEGAR ledger may group executed witnesses by signature, but must retain every unobserved endpoint/path as an unresolved continuous remainder with no Chance mass. Temporary explicit movement-path catalog entries are excluded from the semantic state identity, while exact path and event receipts remain preserved as audit evidence.
- 2026-08-31 durable Warmachine Search participant-source boundary: Search may observe a participant prerequisite such as Heavy Weapon Hardpoint only through the shared Engine Host's exact source contract, legal/rejected action and replay evidence. It must not index that participant as an independent trigger or duplicate True Sight, Guided or Weapon Crate semantics. Slice 23.8w proves Host-only execution for those sources with zero Search-side rule implementations, but does not promote parent 23.8, global Strict, `Q_rule`, values, Skill/training or online experiments.
- 2026-08-31 durable Warmachine exact-grant transport boundary: Search consumes Field Marshal and comparable continuous grants only after the Engine binds one exact primary source to an explicit payload atom/hook allowlist and target scope. A derived grant may not satisfy the primary source contract, and Search may neither infer payload semantics from the grant text nor retain a grant after source or cohort withdrawal. Unsupported payloads remain strict-rejected; non-strict preview evidence is never `Q_rule`, value or training truth.
- 2026-08-25 durable Warmachine activation-domain partition boundary: action-family scope calls may expose optional actions that the unscoped current-window Host suppresses behind a mandatory global window, so a complete cursor must first enumerate the current actor/group window and only then partition that returned set. Control-phase mandatory resolution remains one global slot. A versioned Search family classifier is acceptable only with focused parity against Host family scopes and Host/source receipt binding. Closing every actor-family slot proves only the discrete shell; full unscoped parity, continuous endpoint/path/Unit domains and transition stability are independent gates. Completed pages may resume only after plan, page and slot receipt hashes validate, and operational resume must not change the semantic aggregate receipt.
- 2026-08-25 durable Warmachine connectivity-certificate boundary: a strict endpoint witness may establish nonemptiness but never connectedness by itself. When every active endpoint constraint is an inside relation over a supported convex shape, their finite intersection is convex and one connected component can be certified. Any outside assignment, obstacle/exclusion relation, unsupported shape or missing strict witness remains unresolved. Endpoint-constraint connectivity cannot be promoted to strict reachable-set connectivity, path-class completeness or transition stability. New Ticket 20 execution modules must be explicit execution-receipt roots until production materializers import them transitively.
